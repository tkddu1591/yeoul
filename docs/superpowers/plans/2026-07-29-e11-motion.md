# E11 모션 체계 구현 플랜

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 클릭·호버·화면 전환이 즉시 튀지 않고 짧게 이어지도록 만든다 — 색·투명도·transform만 쓰고 레이아웃은 건드리지 않는다.

**Architecture:** 토큰 + 안전망 테스트 먼저 → 피드백 층(색) → 교체·등장 층(투명도) → 상세 슬롯 높이(grid) → reduced-motion·E2E.

**Tech Stack:** 순수 CSS 커스텀 프로퍼티·`@media (prefers-reduced-motion)`, Vitest 순수 단위(CSS 전수 검사), Playwright Electron E2E.

**게이트 기준선(실측):** 루트 `pnpm test` **529** · `pnpm --filter @git-gui/desktop e2e` **106**(smoke 100 + hosting 6) · typecheck 6/6. **루트에 `build` 스크립트가 없다 — `pnpm --filter @git-gui/desktop build`.**

⚠️ **모든 Playwright 실행은 단일 포그라운드 Bash 호출 + `timeout: 600000`.**
⚠️ **자기가 띄우지 않은 Electron 프로세스는 건드리지 않는다** — 사용자가 개발 앱을 쓰고 있다. **OS 전체 화면 캡처 금지**(과거에 사용자의 무관한 사적 창이 찍힌 적이 있다).

**현황 실측:** `transition:` 선언 **1개**(`ui-button`) · `@keyframes` **1개**(확인창 120ms) · `prefers-reduced-motion` **0건** · **레이아웃 속성에 걸린 transition 0건**(이 상태를 지켜야 한다) · 가상 스크롤 **5곳**. View Transitions는 사용 가능하나(Chromium 134, 기본 전환 297ms 실측) **의도적으로 쓰지 않는다**(스펙 참조 — React `flushSync` 결합 회피).

---

### Task 1: 모션 토큰 + 안전망 테스트 (TDD)

**Files:**
- Modify: `apps/desktop/src/renderer/src/ui/tokens.css`
- Modify: `apps/desktop/src/renderer/src/ui/base.css`
- Create: `apps/desktop/test/motion-tokens.test.ts`

이 태스크의 **핵심 산출물은 안전망 테스트**다. 이 에픽에서 가장 깨지기 쉬운 규칙("레이아웃 속성에 전환을 걸지 않는다")을 사람 주의력이 아니라 게이트로 지킨다.

- [x] **Step 1: 실패하는 테스트 먼저.** `apps/desktop/test/motion-tokens.test.ts` 생성:

```ts
import { readdirSync, readFileSync, statSync } from 'node:fs'
import { join } from 'node:path'
import { describe, expect, it } from 'vitest'

const CSS_ROOT = join(__dirname, '../src/renderer/src')

function cssFiles(dir: string): string[] {
  return readdirSync(dir).flatMap((entry) => {
    const path = join(dir, entry)
    if (statSync(path).isDirectory()) return cssFiles(path)
    return path.endsWith('.css') ? [path] : []
  })
}

const files = cssFiles(CSS_ROOT).map((path) => ({ path, text: readFileSync(path, 'utf8') }))
const tokens = readFileSync(join(CSS_ROOT, 'ui/tokens.css'), 'utf8')

/** 애니메이션하면 매 프레임 레이아웃을 다시 계산해 버벅인다 — 색·투명도·transform만 쓴다 */
const LAYOUT_PROPS = [
  'height',
  'width',
  'top',
  'left',
  'right',
  'bottom',
  'margin',
  'padding',
  'flex-basis',
  'gap',
]

describe('모션 안전망', () => {
  it('레이아웃 속성에 transition을 걸지 않는다 — grid-template-rows만 의도된 예외', () => {
    const offenders: string[] = []
    for (const { path, text } of files) {
      for (const match of text.matchAll(/transition:\s*([^;]+);/g)) {
        const value = match[1]
        // grid-template-rows는 Chromium이 보간하는 몇 안 되는 경로 — 상세 슬롯 열림에 쓴다(E11 스펙)
        const cleaned = value.replace(/grid-template-rows/g, '')
        for (const prop of LAYOUT_PROPS) {
          if (new RegExp(`(^|[\\s,])${prop}([\\s,]|$)`).test(cleaned)) {
            offenders.push(`${path}: transition: ${value}`)
          }
        }
      }
    }
    expect(offenders).toEqual([])
  })

  it('transition: all을 쓰지 않는다 — 의도치 않은 속성까지 걸려 원인 추적이 불가능해진다', () => {
    const offenders = files
      .filter(({ text }) => /transition:\s*all\b/.test(text))
      .map(({ path }) => path)
    expect(offenders).toEqual([])
  })

  it('모션 토큰이 정의돼 있다', () => {
    for (const token of ['--motion-fast', '--motion-base', '--motion-slow', '--ease-out', '--ease-in-out']) {
      expect(tokens).toContain(`${token}:`)
    }
  })

  it('가장 긴 모션도 240ms를 넘지 않는다 — 200ms를 넘기면 부드러운 게 아니라 느린 것이다', () => {
    const durations = [...tokens.matchAll(/--motion-[a-z]+:\s*(\d+)ms/g)].map((m) => Number(m[1]))
    expect(durations.length).toBeGreaterThan(0)
    expect(Math.max(...durations)).toBeLessThanOrEqual(240)
  })

  it('prefers-reduced-motion 전역 차단이 있다', () => {
    const base = readFileSync(join(CSS_ROOT, 'ui/base.css'), 'utf8')
    expect(base).toContain('prefers-reduced-motion')
  })
})
```

- [x] **Step 2: 실패 확인.** `npx vitest run apps/desktop/test/motion-tokens.test.ts` — 토큰 3건·reduced-motion 1건이 실패하고, **레이아웃 검사 2건은 통과**해야 한다(현재 위반 0건). **출력을 그대로 보고한다.** 레이아웃 검사가 처음부터 통과하는 건 정상이다 — 이건 회귀 방지용 안전망이다.

- [x] **Step 3: 안전망이 진짜 무는지 확인(필수).** 아무 CSS에 `transition: height 200ms;`를 임시로 넣고 테스트가 **실패하는지** 본 뒤 되돌린다. `transition: all 100ms;`도 같은 방식으로 1회. **두 출력을 보고한다** — 통과만 하는 검사는 없는 것과 같다.

- [x] **Step 4: 토큰 추가.** `tokens.css`의 `--shadow-1` 선언 **바로 앞**에 추가(모서리 블록 뒤):

```css
  /* 모션 — 부드러움은 "많이 움직이는 것"이 아니라 "짧게 이어지는 것"이다.
     200ms를 넘기면 부드러운 게 아니라 느린 것이라, slow만 예외로 두고 그 이상은 만들지 않는다 */
  --motion-fast: 100ms; /* 호버·눌림·포커스 — 손끝에 붙는 피드백 */
  --motion-base: 150ms; /* 내용 교체 페이드, 등장 */
  --motion-slow: 240ms; /* 크게 움직이는 것 — 상세 슬롯 열림/닫힘 */
  --ease-out: cubic-bezier(0.2, 0, 0, 1); /* 등장·사용자가 시작한 동작 */
  --ease-in-out: cubic-bezier(0.4, 0, 0.2, 1); /* 양방향으로 오가는 상태 */
```

- [x] **Step 5: reduced-motion 전역 차단.** `base.css` **맨 끝**에 추가:

```css
/* OS에서 모션 줄이기를 켠 사용자에게는 전부 끈다 (E11).
   0이 아니라 0.01ms인 이유: transitionend·animationend에 의존하는 코드가 생겨도 이벤트는 계속 발화한다 */
@media (prefers-reduced-motion: reduce) {
  *,
  *::before,
  *::after {
    animation-duration: 0.01ms !important;
    animation-iteration-count: 1 !important;
    transition-duration: 0.01ms !important;
  }
}
```

- [x] **Step 6: 게이트.** `npx vitest run apps/desktop/test/motion-tokens.test.ts` 전건 통과 · 루트 `pnpm test` — **534**(529 + 신규 5). **실제 숫자를 보고한다.**

- [x] **Step 7: Commit**

```bash
git add apps/desktop/src/renderer/src/ui apps/desktop/test/motion-tokens.test.ts
git commit -m "feat(desktop): E11 모션 토큰 + 레이아웃 전환 금지 안전망

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

---

### Task 2: 피드백 층 — 호버·눌림·선택

**Files:** 아래 실측 호버 규칙이 있는 CSS들

가장 큰 "살아 있다"는 인상을 만들면서 비용이 사실상 0인 층. **가상 스크롤 행에도 안전하다** — 색만 바뀌고 위치·크기가 안 변해 재활용과 충돌하지 않는다.

- [x] **Step 1: 대상 확정(실측 목록).** 아래 선택자의 **기본 상태 규칙**(hover가 아니라 그 요소 자체)에 전환을 건다:

| 파일 | 요소 |
| --- | --- |
| `components/changes-panel.css:61` | `.file-row__main` |
| `components/history-panel.css:58` | `.history-item` |
| `components/branches-panel.css:57` | `.branch-row` |
| `components/worktrees-panel.css:34` | `.worktree-row` |
| `components/commit-detail-panel.css:68` | `.commit-file-folder` |
| `ui/context-menu.css:28` | `.ui-context-menu__item` |
| `ui/list-dialog.css:26` | `.ui-list-dialog__option` |
| `components/review-popover.css:69,101` | `.review-popover__pull`, `.review-popover__pull-external` |
| `components/shelf-popover.css:57` | `.shelf-popover__meta` |
| `components/find-bar.css:41` | `.find-bar__nav` |

각 요소의 기본 규칙에 추가:

```css
  transition: background-color var(--motion-fast) var(--ease-out),
    color var(--motion-fast) var(--ease-out),
    border-color var(--motion-fast) var(--ease-out);
```

**`transition: all` 금지**(Task 1의 안전망이 잡는다). `background`가 아니라 **`background-color`**를 쓴다 — `background` 단축은 이미지·위치까지 포함해 의도가 흐려진다. 기존 `ui-button.css`의 `transition: background 0.12s ease…`도 이 관용으로 통일하고 토큰을 쓰게 바꾼다.

- [x] **Step 2: 좌측 탭.** `layout.css`의 좌측 탭 버튼(실독 — `app__tab` 계열)에도 같은 전환을 건다. 탭은 클릭 대상이라 피드백이 특히 눈에 띈다.

- [x] **Step 3: 실측(필수).** Playwright로 실제 앱을 띄워 (a) `.file-row__main`의 계산된 `transition-duration`이 `0.1s`인지 (b) **파일 5000개 저장소**에서 행 위로 마우스를 빠르게 훑을 때 프레임 드랍이 있는지 — 측정 방법을 스스로 정하고 보고한다. 가상 스크롤 행에 색 전환을 거는 것이 안전하다는 이 플랜의 전제를 실제로 확인하는 단계다. 프로브는 삭제.

- [x] **Step 4: 게이트.** typecheck · 루트 **534** 유지 · build · e2e **106 유지**(포그라운드, `timeout: 600000`).

- [x] **Step 5: Commit**

```bash
git add apps/desktop/src/renderer/src
git commit -m "feat(desktop): E11 피드백 층 — 행·메뉴·탭 호버 전환

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

---

### Task 3: 교체 페이드 + 등장

**Files:** `ui/base.css`(공용 키프레임) · `ui/tooltip.css` · `ui/confirm-dialog.css` · `components/find-bar.css` · `layout.css`(알림) · diff·커밋 상세·리뷰 상세 컴포넌트

- [x] **Step 1: 공용 키프레임.** `base.css`의 reduced-motion 블록 **앞**에 추가:

```css
/* E11 — 같은 자리에서 내용만 바뀌는 순간에 건다. 3px은 의도적으로 작다:
   크게 움직이면 "미끄러진다"는 인상이 되어 오히려 느려 보인다 */
@keyframes ui-enter {
  from {
    opacity: 0;
    transform: translateY(3px);
  }
}
```

`to`를 쓰지 않는다 — 생략하면 요소의 실제 최종 상태가 쓰여서 다른 `transform`과 충돌하지 않는다.

- [x] **Step 2: 내용 교체 페이드.** diff 본문·커밋 상세 본문·리뷰 상세 본문 컨테이너에 `animation: ui-enter var(--motion-base) var(--ease-out)`을 걸고, **React `key`를 내용 식별자로 바꿔** 교체 때 재생되게 한다(예: diff는 파일 경로, 커밋 상세는 해시).
  ⚠️ **`key` 변경은 그 하위 트리를 새로 마운트시킨다** — 가상 스크롤의 스크롤 위치가 초기화될 수 있다. 각 대상에서 **스크롤 위치가 리셋돼도 되는지** 판단하고(파일이 바뀌면 맨 위가 맞다), 리셋되면 안 되는 곳에는 걸지 말 것. 판단 근거를 보고한다.
  ⚠️ **로딩 상태에는 걸지 않는다** — 성공 결과에만. `busy` 전환에 페이드가 겹치면 이중 깜빡임이 된다(스펙 에러표).

- [x] **Step 3: 등장.** `.ui-tooltip`·`.ui-dialog`·`.find-bar`·`.app__notice`에 같은 애니메이션을 건다. 확인창의 기존 `ui-fade-in`(120ms)은 **삭제하고** `ui-enter` + 토큰으로 흡수한다(중복 정의를 남기지 않는다).
  **퇴장은 만들지 않는다** — 스펙의 의도된 비대칭.

- [x] **Step 4: 테마 전환 제외 확인.** 다크/라이트 토글 시 전체 화면이 물결치지 않는지 확인한다. 색 전환이 `background-color`에 걸려 있으므로 테마를 바꾸면 모든 행이 동시에 전환될 수 있다 — **실측하고, 물결치면 테마 전환 순간만 전환을 끄는 방법**(예: `documentElement`에 일시 클래스)을 쓴다. 실측 결과를 보고한다.

- [x] **Step 5: 게이트.** typecheck · 루트 534 유지 · build · e2e **106 유지**. 교체 페이드가 `toBeVisible()` 단언과 경합하면 **고정 대기를 넣지 말고** 단언 타임아웃을 올린다.

- [x] **Step 6: Commit**

```bash
git add apps/desktop/src/renderer/src
git commit -m "feat(desktop): E11 내용 교체 페이드 + 등장 애니메이션

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

---

### Task 4: 상세 슬롯 높이 전환 (이 에픽의 최대 위험)

**Files:** `apps/desktop/src/renderer/src/layout.css` · `App.tsx`(구조 변경이 필요하면)

우측 커밋 상세 슬롯만 **진짜 높이 변화**다. 현재(실측 `layout.css:265-284`):

```css
.app__right {
  display: flex;
  flex-direction: column;
  gap: var(--space-4);
  min-height: 0;
}
.app__right > .ui-panel {
  flex: 1;
  min-height: 0;
}
.app__right-detail {
  flex: none;
  height: 45%;
  min-height: 240px;
  display: flex;
  flex-direction: column;
}
```

- [x] **Step 1: grid로 전환.** `.app__right`를 `display: grid`로 바꾸고 행을 `1fr` + 상세 슬롯 `0fr↔1fr`로 둔다. `grid-template-rows` 전환에 `--motion-slow` `--ease-in-out`을 건다. 슬롯 내부는 `overflow: hidden` + `min-height: 0`이라야 `0fr`에서 실제로 접힌다.
  **정확한 CSS는 실독 후 직접 짠다** — 이 플랜이 완본을 주지 않는 유일한 곳이다. 이유: 상세 슬롯은 `height: 45%` + `min-height: 240px`라는 두 제약을 동시에 갖고 있어 grid 행으로 옮기는 방법이 여러 가지이고, 어느 쪽이 기존 배치를 보존하는지는 **측정해야 안다**.

- [x] **Step 2: 배치 무회귀 실측(필수).** 전환 **전후로** 다음을 측정해 표로 보고한다: 상세가 닫힌 상태의 우측 패널 높이, 열린 상태의 패널·슬롯 높이, 최소 창(960×600)에서의 두 값. **이전과 같아야 한다.** 다르면 되돌리고 다른 방법을 쓴다.

- [x] **Step 3: 가상 스크롤 충돌 실측(필수).** 상세 슬롯 안에는 가상 스크롤(`CommitDetailPanel`)이 있고, 열리는 240ms 동안 높이가 계속 변한다 → 가상 스크롤이 매 프레임 재측정한다. **커밋을 클릭해 슬롯을 여는 동안 행이 깨지거나 사라지지 않는지, 열린 뒤 스크롤이 정상인지** 실제 앱에서 확인하고 보고한다. **깨지면 이 태스크를 중단하고 보고하라** — 페이드만 남기고 높이 전환을 포기하는 것이 정답일 수 있다.

- [x] **Step 4: 게이트.** typecheck · 루트 534 · build · e2e **106 유지**. 우측 열 배치를 단언하는 기존 E2E(E6a 계열)가 있으면 특히 주의.

- [x] **Step 5: Commit**

```bash
git add apps/desktop/src/renderer/src
git commit -m "feat(desktop): E11 커밋 상세 슬롯 높이 전환 — grid-template-rows

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

---

**Task 1~4 실행 기록 (소급).**

**Task 1 — 안전망이 진짜 무는지 증명:** `transition: height 200ms` 임시 삽입 → 레이아웃 검사 실패 확인 → `transition: all 100ms`로 교체 → all 검사 실패 확인 → 원복. 레이아웃 검사 2건이 처음부터 통과한 건 정상(위반 0건인 상태를 고정하는 회귀 가드). 루트 529 → **534**(예측 정확).

**Task 2 — 이 플랜의 전제가 실측으로 확인됐다.** "가상 스크롤 행에 색 전환은 안전하다"를 파일 **5000개** 저장소에서 검증: 목록 6회 스윕(85 rAF 샘플) `avgGap 16.67ms` · `maxGap 17.70ms` · **드랍 0/84**. 60fps 예산 안. `.file-row__main` 계산된 `transition-duration` = `0.1s` ×3.

**Task 3 — 테마 전환 물결이 실재했다.** 되돌려서 재현: 토글 직후 **20개 요소가 동시에 색 전환**(약 6프레임/100ms). `theme-switching` 클래스를 이중 rAF로 씌워 억제 → 이후 샘플 전 프레임에서 전환 0건, 클래스는 2프레임 뒤 사라지고 `stillSuppressed: false`(끼여서 안 풀리는 일 없음) 확인. 구현자 판단 기록: `key` 페이드는 diff 본문(경로)·커밋 상세 메시지(해시)·리뷰 타임라인(PR 번호)에만 걸고 **커밋 상세의 가상 스크롤 파일 목록은 제외** — 커밋을 바꿔도 그 목록 스크롤을 리셋하는 기존 동작이 없어서다(diff는 파일이 바뀌면 맨 위가 맞다). 공용 `.virtual-scroll` 클래스 자체에 거는 것도 회피(앱의 모든 가상 목록에 조용히 번진다).

**Task 4 — 구현했다(포기하지 않았다).** 최종 CSS는 두 상태에서 **트랙 함수 모양을 같게** 유지한다(`minmax(0px,0fr)` ↔ `minmax(240px,9fr)`) — 길이·flex가 각각 독립 보간돼 스냅이 없다. `11fr/9fr` = 55/45로 기존 `height:45%`를, `240px`로 `min-height:240px`를 정확히 재현.

- **구현자가 잡은 미묘한 버그:** CSS grid의 `gap`은 트랙이 `0fr`이어도 **무조건 계산된다** — 래퍼를 항상 렌더하면 닫힌 상태에서도 16px을 영구히 잃는다. `gap`을 없애고 열림 상태에서만 상단 패널에 `margin-bottom`을 준다(이 margin은 전환하지 않는다 — 안전망이 금지한다).
- **항상 렌더는 래퍼 div만.** `CommitDetailPanel`(가상 스크롤 보유)은 기존대로 조건부 마운트 — 가상 스크롤이 미리 마운트되지 않고, "닫으면 DOM에서 사라진다"를 단언하는 기존 E2E(`smoke.spec.ts:275`)도 그대로 산다.
- **배치 무회귀 실측(픽셀 일치):** 1280×800 닫힘 694→694 · 열림 366/312→366/312 · 960×600 닫힘 494→494 · 열림 238/240→238/240.
- **가상 스크롤 충돌 실측:** 열림 중 높이가 단조 증가(0→7→30→75→140→200→240→265→312, 스냅 없음), 행은 0 → 15 → 17로 높이에 맞춰 증가(빈 화면 후 튀어나오는 현상 없음), 열린 뒤 첫 행 텍스트·파일 수 정확, 바닥까지 스크롤 후에도 17행 유지, 닫힘도 단조 감소 후 언마운트. 콘솔·페이지 오류 0건. **중단 조건에 걸리지 않았다.**

**후속으로 이월(내가 만든 문제):** E10의 `창이 포커스를 받으면…` 테스트가 **2워커 병렬에서 간헐 실패**한다(5초 타임아웃). 단독 실행은 3/3 통과이고 **수정 전 베이스라인에서도 동일 재현**되므로 이 태스크의 회귀가 아니다 — 원인은 동시에 뜬 두 Electron 창이 **OS 포커스를 두고 경합**하는 것. 내가 E9a·E10에서 넣은 실제 창을 띄우는 테스트의 부작용이다. Task 5에서 처리한다.



### Task 5: E2E + 최종 게이트 + README

**Files:** `apps/desktop/e2e/smoke.spec.ts` · `README.md`

- [ ] **Step 1: E2E 2건.**
  1. **reduced-motion에서 주요 흐름이 그대로 동작한다** — Playwright의 `reducedMotion: 'reduce'` 컨텍스트 옵션을 쓴다. ⚠️ **이 저장소는 `electron.launch`를 쓰므로 브라우저 컨텍스트 옵션을 그대로 못 줄 수 있다.** 실독하고, 안 되면 `page.emulateMedia({ reducedMotion: 'reduce' })`로 간다. 그마저 안 되면 **못 한다고 보고**하고 대안(전환 지속시간을 직접 읽어 0.01ms인지 확인)을 쓴다.
  2. **상세 슬롯 열림/닫힘 후 우측 열 배치가 이전과 같다** — Task 4의 회귀를 고정한다.

- [ ] **Step 2: 각 신규 테스트 `-g` 2회씩** 비플레이키 확인.

- [ ] **Step 3: 최종 게이트.** typecheck 6/6 · 루트 **534** · build · smoke **102**(100+2) · e2e **108** · `last-screen` 아티팩트 0건.

- [ ] **Step 4: 스크린샷 — 모션은 정지 이미지로 안 보인다.** 상세 슬롯이 **열리는 중간 프레임**(약 120ms 시점)을 캡처해 전환이 실제로 일어남을 보인다. 정지 3장: 닫힘 / **열리는 중** / 열림. `/private/tmp/claude-501/-Users-sangyeop-kim-git-gui/b4ef6d32-042d-440c-8252-b8944659aa01/scratchpad/`(하이픈 경로)에 저장하고 `ls`로 존재를 확인해 붙인다.

- [ ] **Step 5: README.** 기존 E10 문장(**실독**) 뒤에 추가:

```markdown
E11: 클릭·호버·화면 전환에 짧은 모션을 넣었습니다(색 100ms · 내용 교체 150ms · 상세 열림 240ms). 색·투명도·transform만 움직여 레이아웃은 흔들리지 않고, OS에서 모션 줄이기를 켜면 전부 꺼집니다.
```

- [ ] **Step 6: Commit**

```bash
git add apps/desktop/e2e/smoke.spec.ts README.md
git commit -m "test(desktop): E11 E2E 2건 — reduced-motion·상세 슬롯 배치 + README

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

---

## 게이트 표 (누적)

| 시점 | 루트 테스트 | smoke | e2e 합 |
| --- | --- | --- | --- |
| 시작 | 529 | 100 | 106 |
| Task 1 후 | +5 → **534** | 100 | 106 |
| Task 2 후 | 534 유지 | 100 유지 | 106 유지 |
| Task 3 후 | 534 유지 | 100 유지 | 106 유지 |
| Task 4 후 | 534 유지 | 100 유지 | 106 유지 |
| Task 5 후 | 534 | +2 → **102** | **108** |

## Self-Review (플랜 자체 점검)

1. **스펙 커버리지**: ①토큰=T1 · ②피드백=T2 · ③교체 페이드=T3 Step 2 · ④등장=T3 Step 3 · ⑤레이아웃 3종=T3 Step 2(탭 크로스페이드)+T4(상세 슬롯)+T3 Step 3(알림) · ⑥reduced-motion=T1 Step 5 + T5 Step 1. 스펙이 **하지 않기로 한 것**(드래그 리사이즈·가상 스크롤 행·퇴장)은 플랜 어디에도 없다 — 의도적. 에러표: E2E 경합=T3 Step 5·T5 Step 2 · grid×가상 스크롤=T4 Step 3(중단 조건 포함) · busy 이중 깜빡임=T3 Step 2 · 테마 물결=T3 Step 4 · 터미널=건드리지 않으므로 무해.
2. **자리표시자**: T4 Step 1이 완본 CSS를 주지 않는다 — **의도적이며 이유를 명시**했다(`height: 45%` + `min-height: 240px` 이중 제약을 grid로 옮기는 방법이 여럿이고 어느 쪽이 배치를 보존하는지는 측정해야 안다). 그 대신 Step 2·3에 **측정 항목과 중단 조건**을 못 박았다. 나머지 코드 블록은 전부 완본.
3. **타입 정합**: CSS·테스트만 다루므로 타입 의존이 없다. T1의 토큰이 T2~T4의 소비보다 먼저다.
4. **알려진 위험**: ① T4의 grid 전환이 가상 스크롤과 충돌하면 **포기하는 것이 정답**일 수 있다 — 중단 조건을 Step 3에 적었다. ② T3 Step 2의 `key` 교체가 하위 트리를 재마운트한다 — 스크롤 리셋 허용 여부를 대상마다 판단하게 했다. ③ Playwright에서 `reducedMotion`을 Electron launch에 못 줄 수 있다 — 3단 폴백을 적었다.
