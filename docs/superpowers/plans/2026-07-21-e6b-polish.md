# E6b 후속 폴리시 묶음 — 역대 리뷰·플랜 후속 노트 선별 6건 구현 계획

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) 구문으로 추적한다.

**Goal:** 역대 플랜·리뷰의 후속 노트에서 선별한 6건을 한 묶음으로 해소한다 — ① 백업(push)이 원격 앞섬으로 거부될 때 원어 에러 대신 받아오기(pull) 안내(undo 직후 포함), ② notice 안내 배너 10초 자동 소멸(에러·머지 바는 유지), ③ 팝오버·PromptDialog ESC 불응 해소(**원인 실측 완료** — 아래 실측 2), ④ 히스토리 태그 배지 🏷 구분 + 우선순위 원격 뒤(파서 `tag: ` 접두 보존), ⑤ E2E 디버깅 경로(`GIT_GUI_E2E_SHOW=1` 창 표시 + 실패 시 마지막 화면 저장), ⑥ 커밋이 삭제한 파일의 "이 파일만 적용" 메뉴 비활성+사유.

**Architecture:** 기존 레이어 경계 그대로 — 엔진 문구 매핑은 git-adapter(client.ts), 파서 구조 확장은 domain+git-adapter(refs 유지 + `tags` 병행 — 기존 소비자 무영향), 타이머·ESC fallback은 renderer(App·ui 훅), E2E 게이트는 main/index.ts env 게이트 관례(`GIT_GUI_E2E_REPO`·`isPackaged`)와 스펙 공통 헬퍼(e2e/harness.ts). 모든 불확실 동작은 사전 실측으로 고정했다(push stderr 4케이스, react-aria ESC 원인 소스+실기동, Playwright page.clock·afterEach 스크린샷의 electron.launch 동작).

**Tech Stack:** 기존 그대로 — TypeScript, Electron(main/preload/renderer), zustand, react-aria-components 1.19.0(react-aria 3.50.0), vitest, Playwright(Electron E2E).

**기준 커밋:** `feature/e6b-polish` = `5bfeefe`. 기준선 실측: 단위 **353 tests**(27 files, `pnpm test` 실행 — 353 passed), E2E **43**(smoke 37 + hosting 6, `grep -c "^test("` 실측).

## 확정 범위 ↔ 출처 후속 노트 대응표

| # | 항목 | 출처 후속 노트 (원문) | 위치 |
| --- | --- | --- | --- |
| 1 | 백업 원격 앞섬 친절화 | "(통합 리뷰 Minor) undo 후 백업(push)이 원어 에러(`failed to push some refs`) — 원격이 앞선 기존 상황과 동일 노출. 친절 매핑 또는 '받아온 뒤 백업' 안내 검토." | `2026-07-21-e5b-history-commit-ops.md:2728` |
| 2 | notice 자동 사라짐 | "알림 자동 사라짐(토스트 타이머) — 현재는 다음 작업까지 유지" | `2026-07-20-e1d-ux-feedback.md:950` |
| 3 | ESC 불응 해소 | "보관함 팝오버: 보관하기 클릭 후 ESC 불응(포커스가 body로 떨어짐 — 바깥 클릭으로만 닫힘)" + "에러로 busy를 거친 뒤 보관함 팝오버 포커스가 body로 빠져 ESC 불응(기존 패턴, E1a 잔여와 동류)" + "(품질 리뷰, 기존 결함) PromptDialog가 ESC로 안 닫힌다(data-exiting조차 미부착 — E1a 잔여 ESC 불응과 동류). 그만두기·바깥 클릭은 정상 — 후속 수리." | `2026-07-17-e1a-branches-shelf.md:1802` · `2026-07-20-e1d-ux-feedback.md:953` · `2026-07-21-e3b-review-flow.md:3058` |
| 4 | 태그 배지 구분 | "태그를 로컬과 구분해 뒤로 보내려면 log 파서가 `tag: ` 접두 정보를 보존해야 한다(CommitSummary.refs 구조 확장) — 우선순위 하위 분류 후속" (E5b 실측 9에서 태그가 배지로 자동 반영되며 구분 부재가 가시화됨) | `2026-07-21-e4-ux-feedback-3.md:377` (+ `:22`) · `2026-07-21-e5b-history-commit-ops.md:34` |
| 5 | E2E 디버깅 경로 | "E2E 숨김 모드 opt-out 부재 — headed로 눈으로 보며 디버깅하고 싶을 때 `GIT_GUI_E2E_SHOW=1` 같은 우회가 없다" + "(통합 리뷰 Minor) electron.launch 테스트에는 trace가 스텁(스크린샷 0장) — 창까지 숨겨진 지금 실패 시 시각 단서 전무." | `2026-07-21-e6a-layout.md` 후속 노트 2건 |
| 6 | 삭제 파일 "이 파일만 적용" 비활성 | "(통합 리뷰 Minor) 커밋이 삭제한 파일에도 '이 파일만 적용' 메뉴가 뜬다(엔진이 친절 거부하긴 함) — disabled 처리 검토." | `2026-07-21-e5a-file-actions.md:2114` |

## 사전 실측 기록 (2026-07-21, macOS Darwin 25.5.0 · git 2.50.1 · Electron 35 · Playwright 1.50 · react-aria-components 1.19.0 · 5bfeefe)

### 실측 1. push 거부 stderr — 로컬 bare remote 4케이스

로컬 bare remote + 별도 클론으로 원격을 앞세운 뒤 재현했다:

| 케이스 | stderr 핵심 줄 | exit |
| --- | --- | --- |
| ① upstream 일치 + 원격 앞섬(fetch 안 함), 평범한 push | `! [rejected]  main -> main (fetch first)` + `error: failed to push some refs to '…'` | 1 |
| ② fetch 후(behind 인지) push | `! [rejected]  main -> main (non-fast-forward)` + 동일 error 줄 | 1 |
| ③ **undo 직후**(pull 동기화 → `reset --mixed HEAD~1` → push) | ②와 동일 — `(non-fast-forward)` | 1 |
| ④ upstream 없음 + 원격 앞섬, `push -u --end-of-options origin HEAD` | `! [rejected]  HEAD -> main (fetch first)` + 동일 error 줄 | 1 |

판정 문자열 선택: `failed to push some refs`는 hook 거부(`! [remote rejected] … (pre-receive hook declined)`)에도 나와 오매핑 위험이 있다 — **괄호 사유 `(fetch first)`·`(non-fast-forward)`로 판정**한다(4케이스 전부 커버, `[remote rejected]`는 두 문자열 모두 없음). **undo 직후는 ③=②와 동일 stderr이므로 같은 문구로 커버된다.**

### 실측 2. react-aria ESC 불응 — 원인 규명 (Electron 실기동 프로브 + 라이브러리 소스)

임시 프로브 스펙(`e2e/probe-esc*.spec.ts` — 실측 후 삭제, 워킹 트리 클린)으로 빌드된 앱을 Playwright로 실기동해 재현·규명했다.

**수정 전 재현 (프로브 결과):**

| 시나리오 | ESC 직전 activeElement | ESC 결과 |
| --- | --- | --- |
| 보관함 팝오버 열자마자 ESC | `SECTION.shelf-popover__dialog` | **닫힘** ✓ |
| 보관함 팝오버 → 보관하기 클릭(busy 비활성화 사이클) → ESC | `BODY` | **불응** ✗ (E1a 잔여 재현) |
| 리뷰 팝오버 열자마자 ESC | `SECTION.review-popover__dialog` | 닫힘 ✓ |
| 리뷰 팝오버 → blur → ESC | `BODY` | **불응** ✗ |
| 스위처 메뉴 열자마자 ESC | `DIV.branch-switcher__menu` | 닫힘 ✓ |
| PromptDialog(스위처 경유·ContextMenu 경유 모두) ESC | `INPUT[prompt-input]` (포커스 정상!) | **불응** ✗ (E3b 리뷰 재현) |
| ConfirmDialog ESC | `SECTION` | 닫힘 ✓ |

**원인 2가지 (서로 다름):**

- **PromptDialog — TextField `onKeyDown`의 기본 전파 차단.** capture-단계 계측으로 keydown이 `INPUT → … → ui-modal → ui-modal-overlay → BODY`까지 native 전파되지만 document bubble에는 도착하지 않음을 확인. react-aria 소스(`react-aria@3.50.0/dist/private/interactions/createEventHandler.mjs`)가 결정적 근거다: 사용자 `onKeyDown`을 감싸는 `createEventHandler`가 핸들러 실행 후 **`continuePropagation()`을 부르지 않으면 무조건 `e.stopPropagation()`** 한다("stopPropagation is now the default behavior for events in React Spectrum"). PromptDialog는 Enter 제출용 `onKeyDown`을 TextField에 넘기므로 **ESC를 포함한 모든 keydown의 전파가 input에서 끊겨** ModalOverlay의 ESC 핸들러(`useOverlay` — 오버레이 DOM의 onKeyDown, `useOverlay.mjs` 실독)에 닿지 않는다. ConfirmDialog·ListDialog는 onKeyDown이 없어 정상인 것과 정합.
- **팝오버 — 포커스가 body로 빠지면 keydown이 오버레이를 안 거친다.** react-aria의 ESC 닫기는 오버레이 DOM 요소에 부착된 onKeyDown이라 **이벤트 경로에 오버레이가 있어야** 동작한다. busy로 포커스된 버튼이 비활성화되면 브라우저가 포커스를 body로 떨어뜨리고(프로브에서 보관하기 클릭 직후 `BODY` 확인), body 타깃 keydown의 전파 경로에는 팝오버가 없어 닫기가 죽는다.

**수정안 실기동 재검증 (임시 적용 → 빌드 → 프로브 재실행 → 원복):** PromptDialog에 `event.continuePropagation()`(미제출 키만), ShelfPopover에 body-포커스 fallback(window keydown)을 임시 적용한 결과 — 위 표의 ✗ 3건 중 PromptDialog 2경로·보관함 body-포커스가 전부 **닫힘**으로 전환(ESC 1회), fallback 미적용이던 리뷰 팝오버 blur 케이스만 불응으로 남아 **수정 대상·기전이 정확함을 교차 확인**했다. `continuePropagation()`은 `@react-types/shared@3.36.0` `KeyboardEvent` 타입에 존재(컴파일 확인).

### 실측 3. Playwright page.clock — Electron 페이지에서 동작

`window.clock.install()` 후 페이지에 건 10초 `setTimeout`이 `clock.fastForward(10_500)`으로 즉시 발화함을 확인(`fired: true`). fastForward 이후에도 UI 상호작용(새로고침 클릭 → 스냅샷 재조회 → 카운트 단언)이 정상 — **notice 자동 소멸 E2E를 실제 10초 대기 없이 검증할 수 있다.**

### 실측 4. 실패 스크린샷 — finally 시점 vs afterEach 시점

의도적 실패 테스트(electron.launch)로 확인:

- **테스트 본문 finally 시점**: `test.info().status === 'passed'`, `errors.length === 0` — **실패를 아직 모른다.** 기존 스펙처럼 finally에서 `app.close()` 하는 구조에서는 "실패일 때만 찍기"가 불가능하다(닫힌 뒤 afterEach에 도달).
- **afterEach 시점**: `testInfo.status === 'failed'` 정확히 전달. close 전에 찍어 둔 숨김 창 스크린샷 121KB(완전한 UI 렌더) 저장 확인.

→ 설계 확정: **close 직전에 항상 마지막 화면을 찍고, afterEach가 성공 시 지운다**(Task 5). 스펙 43건의 launch/close 구조는 그대로 두고 import 한 줄만 harness 래퍼로 바꾼다.

### 실측 5. 기준선

`pnpm test` → **Test Files 27 passed, Tests 353 passed**. E2E `grep -c "^test("`: smoke.spec.ts **37** + hosting.spec.ts **6** = **43**.

## notice 자동 소멸 — 기존 E2E 영향 전수 (spec 실독)

notice 단언 전수(`getByTestId('notice')` grep 14곳)를 실독했다. 판정 기준: 타이머는 notice가 **set된 시점**부터 10초 — 모든 단언이 notice를 만드는 동작 완료 직후(같은 흐름의 1~2 단언 안)에 조회하므로 무영향.

| 위치 | 단언 | notice set → 조회 사이 | 판정 |
| --- | --- | --- | --- |
| smoke:449 | '보관함' (전환 자동 보관) | 전환 완료 단언(헤더) 1개 뒤 | 무영향 |
| smoke:557 | '모두 가져왔어요' | 합치기 직후 첫 단언 | 무영향 |
| smoke:662 | '취소' (합치기 취소) | abort 직후, 카운트 단언 2개 뒤 | 무영향 |
| smoke:687 | '보관함' (스마트 병합) | 합치기 직후 첫 단언 | 무영향 |
| smoke:717 | '받아왔어요' | pull 직후 첫 단언 | 무영향 |
| smoke:740 | '되돌리는 새 저장' | revert 직후 첫 단언 | 무영향 |
| smoke:776 | '되돌리기를 취소' | abort 직후, 머지 바 단언 1개 뒤 | 무영향 |
| smoke:980·981 | '저장 예정에 올려뒀어요'·'보관함' | restore 확인 직후 연속 2단언 | 무영향 |
| smoke:1117 | '가져와 새 저장을 만들었어요' | cherry-pick 직후 첫 단언 | 무영향 |
| smoke:1128 | '가져오기를 취소' | abort 직후, 머지 바 단언 1개 뒤 | 무영향 |
| smoke:1149 | '태그를 만들었어요' | 태그 생성 직후 첫 단언 | 무영향 |
| hosting:284 | '리뷰 요청 #1' | 생성 직후 첫 단언 | 무영향 |
| hosting:469 | '병합했어요' | **최장 체인** — 병합 완료(set) 후 상태 배지·mock 검사·확인창 취소를 거쳐 조회. 로컬 mock이라 합계 1~2초 수준 ≪ 10초 | 무영향 |

새 notice가 오면 값 변화로 타이머가 리셋되고, 모든 작업은 guard가 시작 시 `notice: null`을 세팅하므로 **같은 문구가 연속으로 떠도** null→값 전이로 타이머가 다시 걸린다(설계 근거 — Task 3).

## refs 구조 변경 — 파급 전수 (코드 실독)

선택한 설계: **CommitSummary.refs는 한 글자도 안 바꾸고(태그 이름 포함 그대로), `tags: string[]`(refs 중 태그인 이름)를 병행**한다. `refs: string[]` 소비자 전수(`grep -rn "\.refs"` + 타입 사용처):

| 소비자 | 사용 | 영향 |
| --- | --- | --- |
| `HistoryPanel.tsx:185` (switchTarget — 우클릭 이동 대상) | `commit.refs.find(...)` — localBranches 교차 검사 | 무영향 (refs 내용 불변) |
| `HistoryPanel.tsx:316` (배지) | `arrangeRefs(commit.refs, currentBranch)` | **의도 변경 지점** — tags 인자 추가 (Task 2) |
| `history-graph.ts` (레인 그래프) | parents만 사용, refs 미사용 | 무영향 |
| `repository-store.ts` | CommitSummary를 통째로 보관/전달만 | 무영향 |
| `ipc-contract`·`preload`·`git-handlers` | 타입 재수출·구조적 통과(반환 검증 없음 — `git-handlers.ts:319` 실독) | 무영향 (typecheck가 게이트) |
| `commit-detail-parser`(CommitDetail) | refs 필드 자체가 없음 | 무영향 |
| 테스트 fixture: `log-parser.test.ts`(toEqual 전체 객체·refs 단언), `history-graph.test.ts:12`(commit 헬퍼) | **필수 필드 추가로 컴파일·toEqual 깨짐** | Task 2에서 `tags` 추가 갱신 |
| E2E 배지 텍스트 단언: smoke '☁ origin/main'(1071)·'v1.0'(1151) | 렌더 문자열 | ☁ 무변 ✓ / 태그는 `🏷 v1.0`으로 바뀌므로 **1151을 강화 교체** (Task 2) |

알려진 한계(후속 노트 이관): 같은 이름의 브랜치와 태그가 한 커밋에 함께 장식되면(`main, tag: main`) refs에 동명 2건이 생겨 React key 중복 — **기존에도 동일**하게 존재하던 한계로, 이번 범위 밖.

---

### Task 1: 백업(push) 원격 앞섬 친절화 (엔진)

**Files:**
- Test: `packages/git-adapter/test/client.test.ts` (+3)
- Modify: `packages/git-adapter/src/client.ts`

- [x] **Step 1: Red — 실패 테스트 3건.** `packages/git-adapter/test/client.test.ts`의 기존:

```ts
  it('push — detached HEAD에서는 읽히는 에러를 던진다', async () => {
    const { repo } = await createFixtureRepoWithRemote()
    await execGitOrThrow(['checkout', '--detach'], { cwd: repo })
    await expect(createGitClient(repo).sync.push()).rejects.toThrow('브랜치가 아닌 시점')
  })
```

바로 뒤에 추가:

```ts
  it('push — 원격이 앞서 있으면 받아오기 안내로 거부한다 (실측 ①: fetch first)', async () => {
    const { repo, remote } = await createFixtureRepoWithRemote()
    const client = createGitClient(repo)
    await client.sync.push()
    // 다른 클론이 원격에 새 저장을 올린다 — 로컬은 fetch 없이 뒤처진 상태
    const other = await mkdtemp(join(tmpdir(), 'git-gui-other-'))
    await execGitOrThrow(['clone', remote, other], { cwd: tmpdir() })
    await writeFixtureFile(other, 'b.txt', 'o\n')
    await execGitOrThrow(['add', '-A'], { cwd: other })
    await execGitOrThrow([...FIXTURE_IDENT, 'commit', '-m', 'other work'], { cwd: other })
    await execGitOrThrow(['push'], { cwd: other })
    await writeFixtureFile(repo, 'c.txt', 'l\n')
    await client.changes.stage(['c.txt'])
    await client.commits.create('로컬 저장')
    await expect(client.sync.push()).rejects.toThrow(
      '원격에 새 저장이 있어요. 먼저 받아오기(pull)로 합친 뒤 백업해 주세요.',
    )
  })

  it('push — 실행취소(undo)로 로컬이 뒤로 가도 같은 안내로 거부한다 (실측 ③: non-fast-forward)', async () => {
    const { repo } = await createFixtureRepoWithRemote()
    const client = createGitClient(repo)
    await writeFixtureFile(repo, 'b.txt', '1\n')
    await client.changes.stage(['b.txt'])
    await client.commits.create('둘째')
    await client.sync.push()
    const head = (await execGitOrThrow(['rev-parse', 'HEAD'], { cwd: repo })).stdout.trim()
    await client.commits.undoLast(head)
    await expect(client.sync.push()).rejects.toThrow(
      '원격에 새 저장이 있어요. 먼저 받아오기(pull)로 합친 뒤 백업해 주세요.',
    )
  })

  it('push — 첫 연결(-u) 경로에서도 원격이 앞서면 같은 안내로 거부한다 (실측 ④)', async () => {
    const { repo, remote } = await createFixtureRepoWithRemote()
    const client = createGitClient(repo)
    // upstream을 만들기 전에 다른 클론이 원격을 먼저 채운다 — 로컬과 갈라진 역사
    const other = await mkdtemp(join(tmpdir(), 'git-gui-other-'))
    await execGitOrThrow(['clone', remote, other], { cwd: tmpdir() })
    await writeFixtureFile(other, 'b.txt', 'o\n')
    await execGitOrThrow(['add', '-A'], { cwd: other })
    await execGitOrThrow([...FIXTURE_IDENT, 'commit', '-m', 'other first'], { cwd: other })
    await execGitOrThrow(['push', '-u', 'origin', 'main'], { cwd: other })
    await expect(client.sync.push()).rejects.toThrow(
      '원격에 새 저장이 있어요. 먼저 받아오기(pull)로 합친 뒤 백업해 주세요.',
    )
  })
```

- [x] **Step 2: Red 확인** — `pnpm vitest run --project git-adapter -t 'push'` 실행. 신규 3건이 **GitError(원어 stderr) 때문에 기대 메시지 불일치로 실패**하는지 확인(기존 push 테스트 5건은 통과 유지).

- [x] **Step 3: 구현.** `packages/git-adapter/src/client.ts` 기존:

```ts
      async push() {
        const cwd = await topLevel()
        const remotes = await execGitOrThrow(['remote'], { cwd })
        const remoteNames = remotes.stdout
          .trim()
          .split('\n')
          .filter((name) => name !== '')
        if (remoteNames.length === 0) {
          throw new Error('백업할 원격 저장소가 없어요. 먼저 원격 저장소를 연결해 주세요.')
        }
        // 사용자 직관대로 origin을 우선하고, 없으면 (git remote 출력 = 알파벳순) 첫 remote
        const targetRemote = remoteNames.includes('origin') ? 'origin' : remoteNames[0]!
        const branch = await execGit(['symbolic-ref', '-q', '--short', 'HEAD'], { cwd })
        const upstream = await execGit(
          ['rev-parse', '--abbrev-ref', '--symbolic-full-name', '@{upstream}'],
          { cwd },
        )
        // upstream이 "현재 이름과 같은" 원격 브랜치일 때만 평범한 push —
        // rename 뒤에는 옛 이름의 upstream이 남아(git branch -m이 merge ref 유지 — 통합 리뷰 실측)
        // 평범한 push가 원어 에러로 죽는다. 그 경우 아래의 -u 재연결 경로로 태운다
        if (
          upstream.exitCode === 0 &&
          branch.exitCode === 0 &&
          upstream.stdout.trim().endsWith(`/${branch.stdout.trim()}`)
        ) {
          // push.default=matching 같은 사용자 전역 설정이 다른 브랜치까지 올리지 않게 고정한다
          await execGitOrThrow(['-c', 'push.default=simple', 'push'], { cwd })
          return
        }
        // 아직 커밋이 없으면 올릴 것이 없다 — 원문 git 에러 대신 읽히는 메시지로
        const head = await execGit(['rev-parse', '-q', '--verify', 'HEAD'], { cwd })
        if (head.exitCode !== 0) {
          throw new Error('아직 저장된 시점이 없어요. 먼저 저장(commit)한 뒤 백업해 주세요.')
        }
        // detached HEAD에서는 올릴 브랜치가 없다 — 원문 git 에러 대신 읽히는 메시지로
        if (branch.exitCode !== 0) {
          throw new Error('지금은 브랜치가 아닌 시점에 있어요. 브랜치로 이동한 뒤 백업해 주세요.')
        }
        // 첫 백업(또는 이름이 어긋난 upstream 재연결) — 현재 브랜치를 remote에 연결하며 올린다.
        // --end-of-options: 대시로 시작하는 remote 이름이 플래그로 해석되는 것을 차단
        await execGitOrThrow(['push', '-u', '--end-of-options', targetRemote, 'HEAD'], { cwd })
      },
```

교체:

```ts
      async push() {
        const cwd = await topLevel()
        const remotes = await execGitOrThrow(['remote'], { cwd })
        const remoteNames = remotes.stdout
          .trim()
          .split('\n')
          .filter((name) => name !== '')
        if (remoteNames.length === 0) {
          throw new Error('백업할 원격 저장소가 없어요. 먼저 원격 저장소를 연결해 주세요.')
        }
        // 사용자 직관대로 origin을 우선하고, 없으면 (git remote 출력 = 알파벳순) 첫 remote
        const targetRemote = remoteNames.includes('origin') ? 'origin' : remoteNames[0]!
        // 원격이 앞서 거부된 push (E5b 후속) — 실측 stderr 4케이스 전부 "! [rejected] …
        // (fetch first|non-fast-forward)". undo(실행취소)로 로컬이 뒤로 간 경우도 같은
        // non-fast-forward 거부라 같은 문구로 커버된다. 'failed to push some refs'는 hook
        // 거부([remote rejected])에도 나와 쓰지 않는다 — 괄호 사유로만 판정한다
        const rejectIfRemoteAhead = (result: GitResult): void => {
          if (
            result.stderr.includes('(fetch first)') ||
            result.stderr.includes('(non-fast-forward)')
          ) {
            throw new Error('원격에 새 저장이 있어요. 먼저 받아오기(pull)로 합친 뒤 백업해 주세요.')
          }
        }
        const branch = await execGit(['symbolic-ref', '-q', '--short', 'HEAD'], { cwd })
        const upstream = await execGit(
          ['rev-parse', '--abbrev-ref', '--symbolic-full-name', '@{upstream}'],
          { cwd },
        )
        // upstream이 "현재 이름과 같은" 원격 브랜치일 때만 평범한 push —
        // rename 뒤에는 옛 이름의 upstream이 남아(git branch -m이 merge ref 유지 — 통합 리뷰 실측)
        // 평범한 push가 원어 에러로 죽는다. 그 경우 아래의 -u 재연결 경로로 태운다
        if (
          upstream.exitCode === 0 &&
          branch.exitCode === 0 &&
          upstream.stdout.trim().endsWith(`/${branch.stdout.trim()}`)
        ) {
          // push.default=matching 같은 사용자 전역 설정이 다른 브랜치까지 올리지 않게 고정한다
          const plain = await execGit(['-c', 'push.default=simple', 'push'], { cwd })
          if (plain.exitCode !== 0) {
            rejectIfRemoteAhead(plain)
            throw new GitError(['-c', 'push.default=simple', 'push'], plain)
          }
          return
        }
        // 아직 커밋이 없으면 올릴 것이 없다 — 원문 git 에러 대신 읽히는 메시지로
        const head = await execGit(['rev-parse', '-q', '--verify', 'HEAD'], { cwd })
        if (head.exitCode !== 0) {
          throw new Error('아직 저장된 시점이 없어요. 먼저 저장(commit)한 뒤 백업해 주세요.')
        }
        // detached HEAD에서는 올릴 브랜치가 없다 — 원문 git 에러 대신 읽히는 메시지로
        if (branch.exitCode !== 0) {
          throw new Error('지금은 브랜치가 아닌 시점에 있어요. 브랜치로 이동한 뒤 백업해 주세요.')
        }
        // 첫 백업(또는 이름이 어긋난 upstream 재연결) — 현재 브랜치를 remote에 연결하며 올린다.
        // --end-of-options: 대시로 시작하는 remote 이름이 플래그로 해석되는 것을 차단
        const linked = await execGit(['push', '-u', '--end-of-options', targetRemote, 'HEAD'], {
          cwd,
        })
        if (linked.exitCode !== 0) {
          rejectIfRemoteAhead(linked)
          throw new GitError(['push', '-u', '--end-of-options', targetRemote, 'HEEAD'], linked)
        }
      },
```

주의: 위 교체 코드의 마지막 GitError 인자 오타(`'HEEAD'`)는 존재하면 안 된다 — **`'HEAD'`가 맞다**. 구현 시 아래의 올바른 줄로 쓴다:

```ts
          throw new GitError(['push', '-u', '--end-of-options', targetRemote, 'HEAD'], linked)
```

(`GitResult`·`GitError`는 client.ts에 이미 import되어 있다 — 추가 import 불필요.)

- [x] **Step 4: Green** — `pnpm vitest run --project git-adapter` 전체 통과(+3 → git-adapter 파일 내 push 테스트 8건). 루트 `pnpm test` → **356 passed**.

- [x] **Step 5: Commit**

```bash
git add packages/git-adapter/src/client.ts packages/git-adapter/test/client.test.ts
git commit -m "fix(git-adapter): E6b 백업 원격 앞섬 친절화 — 받아오기 안내 (fetch first·non-fast-forward 실측 매핑, undo 직후 포함)

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>"
```

---

### Task 2: 태그 배지 구분 — 파서 `tags` 보존·🏷·우선순위 원격 뒤

**Files:**
- Modify: `packages/domain/src/repository.ts` (CommitSummary.tags)
- Modify: `packages/git-adapter/src/log-parser.ts`
- Test: `packages/git-adapter/test/log-parser.test.ts`
- Modify: `apps/desktop/src/renderer/src/components/history-refs.ts`
- Test: `apps/desktop/test/history-refs.test.ts`, `apps/desktop/test/history-graph.test.ts`(fixture 갱신)
- Modify: `apps/desktop/src/renderer/src/components/HistoryPanel.tsx`
- Modify: `apps/desktop/e2e/smoke.spec.ts` (태그 E2E 단언 강화)

- [x] **Step 1: 타입.** `packages/domain/src/repository.ts` 기존:

```ts
  /** 이 커밋을 가리키는 브랜치·태그 이름들 (%D에서 "HEAD -> "·"tag: " 접두사 제거). 없으면 빈 배열 */
  refs: string[]
```

교체:

```ts
  /** 이 커밋을 가리키는 브랜치·태그 이름들 (%D에서 "HEAD -> "·"tag: " 접두사 제거). 없으면 빈 배열 */
  refs: string[]
  /** refs 중 태그인 이름들 — 배지 모양(🏷)·우선순위 하위 분류용. refs에도 그대로 포함된다 (E4 후속, E6b) */
  tags: string[]
```

- [x] **Step 2: 파서 Red.** `packages/git-adapter/test/log-parser.test.ts` — 세 군데 수정 + 신규 1건.

(a) 기존:

```ts
        committedAt: 1752561600,
        refs: [],
        parents: ['b'.repeat(40)],
```

교체:

```ts
        committedAt: 1752561600,
        refs: [],
        tags: [],
        parents: ['b'.repeat(40)],
```

(b) 기존:

```ts
    const commits = parseLog(raw)
    expect(commits[0]?.refs).toEqual(['main', 'origin/main', 'v1.0'])
    expect(commits[1]?.refs).toEqual([])
```

교체:

```ts
    const commits = parseLog(raw)
    expect(commits[0]?.refs).toEqual(['main', 'origin/main', 'v1.0'])
    // tag: 접두는 벗기되 tags로 보존한다 — refs에는 그대로 남는다 (E6b)
    expect(commits[0]?.tags).toEqual(['v1.0'])
    expect(commits[1]?.refs).toEqual([])
    expect(commits[1]?.tags).toEqual([])
```

(c) 기존:

```ts
  it('refs — shallow clone의 pseudo-decoration grafted는 배지가 아니다', () => {
    const raw = record('a'.repeat(40), 'aaaaaaa', 'A', '100', 'grafted, HEAD -> main', '', 'x') + '\0'
    expect(parseLog(raw)[0]?.refs).toEqual(['main'])
  })
```

교체(같은 테스트에 tags 단언 보강 + 신규 테스트 추가):

```ts
  it('refs — shallow clone의 pseudo-decoration grafted는 배지가 아니다', () => {
    const raw = record('a'.repeat(40), 'aaaaaaa', 'A', '100', 'grafted, HEAD -> main', '', 'x') + '\0'
    expect(parseLog(raw)[0]?.refs).toEqual(['main'])
    expect(parseLog(raw)[0]?.tags).toEqual([])
  })

  it('tags — 태그가 여럿이어도 각각 tags에 담기고 순서는 refs와 동일하다', () => {
    const raw =
      record('a'.repeat(40), 'aaaaaaa', 'A', '100', 'tag: v2.0, main, tag: v1.0', '', 'x') + '\0'
    const commit = parseLog(raw)[0]
    expect(commit?.refs).toEqual(['v2.0', 'main', 'v1.0'])
    expect(commit?.tags).toEqual(['v2.0', 'v1.0'])
  })
```

- [x] **Step 3: Red 확인** — `pnpm vitest run --project git-adapter -t 'refs'` + `-t 'tags'`: 컴파일 에러(tags 필드 부재) 또는 단언 실패를 확인.

- [x] **Step 4: 파서 구현.** `apps/desktop`이 아닌 `packages/git-adapter/src/log-parser.ts` — 파일 전체를 다음으로 교체:

```ts
import type { CommitSummary } from '@git-gui/domain'

const FIELD_SEPARATOR = '\x1f'

interface ParsedDecoration {
  refs: string[]
  tags: string[]
}

/**
 * `%D` 장식 문자열을 이름 배열로 정리한다.
 * "HEAD -> main, origin/main, tag: v1" → refs ['main', 'origin/main', 'v1'], tags ['v1'].
 * `tag: ` 접두는 벗기되 tags로 따로 보존한다 — 배지 모양(🏷)·우선순위 하위 분류용 (E4 후속, E6b).
 * detached HEAD의 단독 "HEAD"와 shallow clone의 pseudo-decoration "grafted"는
 * ref가 아니므로 제외한다 (origin/HEAD·replace ref는 log 인자에서 장식 제외).
 */
function parseRefs(decoration: string): ParsedDecoration {
  const refs: string[] = []
  const tags: string[] = []
  if (decoration === '') return { refs, tags }
  for (const entry of decoration.split(', ')) {
    let ref = entry
    if (ref.startsWith('HEAD -> ')) ref = ref.slice('HEAD -> '.length)
    const isTag = ref.startsWith('tag: ')
    if (isTag) ref = ref.slice('tag: '.length)
    if (ref === 'HEAD' || ref === 'grafted') continue
    refs.push(ref)
    if (isTag) tags.push(ref)
  }
  return { refs, tags }
}

/**
 * `git log --format=%H%x1f%h%x1f%an%x1f%ct%x1f%D%x1f%P%x1f%s -z` 출력을 파싱한다.
 * 레코드는 NUL, 필드는 US(0x1f)로 구분된다. %s(subject)는 git이 한 줄로 정리해 준다.
 * 기형 레코드는 추측해 채우지 않고 건너뛴다.
 */
export function parseLog(rawOutput: string): CommitSummary[] {
  const records = rawOutput.split('\0')
  if (records.length > 0 && records[records.length - 1] === '') records.pop()

  const commits: CommitSummary[] = []
  for (const record of records) {
    const fields = record.split(FIELD_SEPARATOR)
    if (fields.length < 7) continue
    const committedAt = Number(fields[3])
    if (!Number.isFinite(committedAt)) continue
    const { refs, tags } = parseRefs(fields[4]!)
    commits.push({
      hash: fields[0]!,
      shortHash: fields[1]!,
      authorName: fields[2]!,
      committedAt,
      refs,
      tags,
      parents: fields[5]! === '' ? [] : fields[5]!.split(' '),
      // subject에 구분자가 섞이는 일은 없지만 방어적으로 나머지를 합친다
      subject: fields.slice(6).join(FIELD_SEPARATOR),
    })
  }
  return commits
}
```

- [x] **Step 5: fixture 컴파일 수선.** `apps/desktop/test/history-graph.test.ts` 기존:

```ts
    committedAt: 0,
    refs: [],
    parents,
```

교체:

```ts
    committedAt: 0,
    refs: [],
    tags: [],
    parents,
```

- [x] **Step 6: history-refs Red.** `apps/desktop/test/history-refs.test.ts` — 파일 전체를 다음으로 교체:

```ts
import { describe, expect, it } from 'vitest'
import { arrangeRefs, isRemoteRef, refBadgeLabel } from '../src/renderer/src/components/history-refs'

describe('arrangeRefs', () => {
  it('현재 브랜치 > 로컬 > 원격(origin/) > 태그 순으로 정렬하고, 접힘이 생기면 1개만 보여준다', () => {
    const result = arrangeRefs(['origin/main', 'feature/login', 'main', 'v1.0'], 'main', ['v1.0'])
    // 3개 이상이면 배지끼리 폭을 나눠 갖다 전부 말줄임된다 — 최상위 1개만 온전히 (품질 리뷰 실측)
    expect(result.visible).toEqual(['main'])
    // 태그는 원격 뒤 — E4 때 파서가 접두를 벗겨 불가했던 하위 분류 (E6b tags로 완성)
    expect(result.hidden).toEqual(['feature/login', 'origin/main', 'v1.0'])
  })

  it('tags를 안 넘기면 기존 동작 그대로다 (태그가 로컬과 동급 — 하위 호환)', () => {
    const result = arrangeRefs(['origin/main', 'feature/login', 'main', 'v1.0'], 'main')
    expect(result.visible).toEqual(['main'])
    expect(result.hidden).toEqual(['feature/login', 'v1.0', 'origin/main'])
  })

  it('2개 이하면 전부 보이고 접힘이 없다', () => {
    expect(arrangeRefs(['main'], 'main')).toEqual({ visible: ['main'], hidden: [] })
    expect(arrangeRefs([], null)).toEqual({ visible: [], hidden: [] })
  })

  it('현재 브랜치가 없으면 로컬 우선 정렬만 적용한다', () => {
    const result = arrangeRefs(['origin/a', 'b', 'origin/c', 'd'], null)
    expect(result.visible).toEqual(['b'])
    expect(result.hidden).toEqual(['d', 'origin/a', 'origin/c'])
  })

  it('같은 우선순위 안에서는 입력 순서를 유지한다 (안정 정렬)', () => {
    const result = arrangeRefs(['z-branch', 'a-branch', 'main'], 'main')
    expect(result.visible).toEqual(['main'])
    expect(result.hidden).toEqual(['z-branch', 'a-branch'])
  })
})

describe('isRemoteRef', () => {
  it('origin/ 접두만 원격으로 본다 (E4 휴리스틱 — refPriority와 동일 기준)', () => {
    expect(isRemoteRef('origin/main')).toBe(true)
    expect(isRemoteRef('main')).toBe(false)
  })

  it('폴더형 로컬 이름(feature/a)은 원격이 아니다', () => {
    expect(isRemoteRef('feature/login')).toBe(false)
    expect(isRemoteRef('origin/feature/login')).toBe(true)
  })
})

describe('refBadgeLabel', () => {
  it('태그는 🏷, 원격은 ☁ 접두 — 로컬은 민무늬 (3분 구분)', () => {
    expect(refBadgeLabel('v1.0', ['v1.0'])).toBe('🏷 v1.0')
    expect(refBadgeLabel('origin/main', [])).toBe('☁ origin/main')
    expect(refBadgeLabel('main', [])).toBe('main')
  })

  it('태그 판정이 원격 휴리스틱보다 우선한다 (origin/ 모양의 태그 이름)', () => {
    expect(refBadgeLabel('origin/x', ['origin/x'])).toBe('🏷 origin/x')
  })
})
```

- [x] **Step 7: Red 확인** — `pnpm vitest run --project desktop -t 'refBadgeLabel'`: refBadgeLabel 부재로 실패 확인.

- [x] **Step 8: history-refs 구현.** `apps/desktop/src/renderer/src/components/history-refs.ts` — 파일 전체를 다음으로 교체:

```ts
export interface ArrangedRefs {
  /** 보여줄 배지 — 우선순위 상위 (현재 브랜치 > 로컬 > 원격 > 태그) */
  visible: string[]
  /** "+N"으로 접히는 나머지 — 툴팁으로만 보여준다 */
  hidden: string[]
}

/** 원격 ref 추정 — decorate 출력의 원격은 "<remote>/…" 형태다. origin 우선 규칙(push)과 동일 계열 휴리스틱 */
function refPriority(ref: string, currentBranch: string | null, tags: string[]): number {
  if (ref === currentBranch) return 0
  // 태그는 원격 뒤 — E4 때 파서가 tag: 접두를 벗겨 불가했던 하위 분류 (E6b tags로 완성)
  if (tags.includes(ref)) return 3
  if (!ref.includes('/')) return 1
  if (ref.startsWith('origin/')) return 2
  // 슬래시가 있지만 origin/이 아닌 것 — 로컬 폴더형(feature/a) 또는 다른 원격. 로컬 쪽에 가깝게 둔다
  return 1
}

/**
 * ref 배지를 우선순위로 정렬해 상위 max개만 보이게 나눈다 (피드백: 여러 개·긴 이름이면 전부 죽는다).
 * 같은 우선순위 안에서는 입력 순서를 유지한다(안정 정렬 — 예측 가능성).
 */
export function arrangeRefs(
  refs: string[],
  currentBranch: string | null,
  tags: string[] = [],
  max = 2,
): ArrangedRefs {
  const sorted = refs
    .map((ref, index) => ({ ref, index, priority: refPriority(ref, currentBranch, tags) }))
    .sort((a, b) => a.priority - b.priority || a.index - b.index)
    .map((entry) => entry.ref)
  // 접힘이 생기는 행은 1개만 보여준다 — 배지끼리 폭을 나눠 갖다 전부 "ma…"로 죽는 것을 막는다 (품질 리뷰 실측)
  const effectiveMax = sorted.length > max ? 1 : max
  return { visible: sorted.slice(0, effectiveMax), hidden: sorted.slice(effectiveMax) }
}

/**
 * 원격 ref 추정 — decorate 출력의 원격은 "origin/…" 형태다(E4 관례 휴리스틱, refPriority와 동일 기준).
 * 폴더형 로컬 이름(feature/a)과의 구분은 origin/ 접두만 신뢰한다
 */
export function isRemoteRef(ref: string): boolean {
  return ref.startsWith('origin/')
}

/** 배지 표시명 — 태그 🏷, 원격 ☁ 접두로 로컬(민무늬)과 3분 구분한다. 태그 판정이 우선 (E6b) */
export function refBadgeLabel(ref: string, tags: string[]): string {
  if (tags.includes(ref)) return `🏷 ${ref}`
  if (isRemoteRef(ref)) return `☁ ${ref}`
  return ref
}
```

- [x] **Step 9: HistoryPanel 배선.** `apps/desktop/src/renderer/src/components/HistoryPanel.tsx` 기존:

```ts
import { arrangeRefs, isRemoteRef } from './history-refs'
```

교체:

```ts
import { arrangeRefs, isRemoteRef, refBadgeLabel } from './history-refs'
```

그리고 기존:

```tsx
                        {(() => {
                          // 배지 폭 경쟁으로 전부 말줄임되는 것을 막는다 — 상위 2개 + "+N" 접기 (피드백)
                          const arranged = arrangeRefs(commit.refs, currentBranch)
                          return (
                            <>
                              {arranged.visible.map((ref) => (
                                <span
                                  key={ref}
                                  title={ref}
                                  className={[
                                    'history-item__ref',
                                    ref === currentBranch ? 'history-item__ref--head' : '',
                                    // 원격은 ☁ 접두 + 점선으로 구분한다 (피드백 3)
                                    isRemoteRef(ref) ? 'history-item__ref--remote' : '',
                                  ]
                                    .filter(Boolean)
                                    .join(' ')}
                                >
                                  {isRemoteRef(ref) ? `☁ ${ref}` : ref}
                                </span>
                              ))}
                              {arranged.hidden.length > 0 && (
                                <span
                                  className="history-item__ref history-item__ref--more"
                                  title={arranged.hidden
                                    .map((ref) => (isRemoteRef(ref) ? `☁ ${ref}` : ref))
                                    .join('\n')}
                                  data-testid={`history-refs-more-${commit.hash}`}
                                >
                                  +{arranged.hidden.length}
                                </span>
                              )}
                            </>
                          )
                        })()}
```

교체:

```tsx
                        {(() => {
                          // 배지 폭 경쟁으로 전부 말줄임되는 것을 막는다 — 상위 2개 + "+N" 접기 (피드백)
                          const arranged = arrangeRefs(commit.refs, currentBranch, commit.tags)
                          return (
                            <>
                              {arranged.visible.map((ref) => (
                                <span
                                  key={ref}
                                  title={ref}
                                  className={[
                                    'history-item__ref',
                                    ref === currentBranch ? 'history-item__ref--head' : '',
                                    // 원격은 ☁ 접두 + 점선, 태그는 🏷 접두(실선 유지) — 3분 구분 (E4·E5b 후속)
                                    !commit.tags.includes(ref) && isRemoteRef(ref)
                                      ? 'history-item__ref--remote'
                                      : '',
                                  ]
                                    .filter(Boolean)
                                    .join(' ')}
                                >
                                  {refBadgeLabel(ref, commit.tags)}
                                </span>
                              ))}
                              {arranged.hidden.length > 0 && (
                                <span
                                  className="history-item__ref history-item__ref--more"
                                  title={arranged.hidden
                                    .map((ref) => refBadgeLabel(ref, commit.tags))
                                    .join('\n')}
                                  data-testid={`history-refs-more-${commit.hash}`}
                                >
                                  +{arranged.hidden.length}
                                </span>
                              )}
                            </>
                          )
                        })()}
```

(CSS는 무변 — 로컬=민무늬 실선, 원격=☁+점선(`--remote` 기존 규칙), 태그=🏷+실선. 글리프가 1차 신호, 테두리 모양이 보조 — 색에 의존하지 않아 색약 대응 관례 유지.)

- [x] **Step 10: E2E 태그 단언 강화.** `apps/desktop/e2e/smoke.spec.ts` 기존:

```ts
    // 태그는 --all 그래프의 decorate 배지로 자동 반영된다 (실측 9)
    await expect(window.getByTestId('history-list')).toContainText('v1.0')
```

교체:

```ts
    // 태그는 --all 그래프의 decorate 배지로 자동 반영된다 (실측 9) — 🏷 접두로 로컬·원격과 구분 (E6b)
    await expect(window.getByTestId('history-list')).toContainText('🏷 v1.0')
```

- [x] **Step 11: Green + 게이트** — 루트 `pnpm test` → **359 passed**(log-parser +1, history-refs +2). `pnpm typecheck` 전부 Done. `cd apps/desktop && npx electron-vite build && npx playwright test e2e/smoke.spec.ts` → **38 passed**(강화된 태그 테스트 포함 — 이 시점 smoke는 아직 37 + Task 3 이전이므로 37. 강화 단언만 통과 확인).

  주의: 이 시점의 smoke 개수는 **37**이다(신규 테스트는 Task 3·4·6에서 추가).

- [x] **Step 12: Commit**

```bash
git add packages/domain/src/repository.ts packages/git-adapter/src/log-parser.ts packages/git-adapter/test/log-parser.test.ts apps/desktop/src/renderer/src/components/history-refs.ts apps/desktop/src/renderer/src/components/HistoryPanel.tsx apps/desktop/test/history-refs.test.ts apps/desktop/test/history-graph.test.ts apps/desktop/e2e/smoke.spec.ts
git commit -m "feat: E6b 태그 배지 — 파서 tags 보존(refs 무변 병행)·🏷 3분 구분·우선순위 원격 뒤

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>"
```

---

### Task 3: notice 10초 자동 소멸 — 타이머는 renderer(App)

설계 판단: 타이머를 **App(오버레이 스택 소유자)**에 둔다 — store는 순수 상태 유지(`clearNotice`만 추가), `setTimeout` 같은 부수효과는 렌더 계층의 effect가 담당(레이어 분리 관례). 리셋 규칙은 공짜로 성립한다: 모든 작업은 `guard`가 시작 시 `notice: null`을 세팅하므로 **같은 문구가 연속으로 와도** null→값 전이로 effect가 다시 걸린다(위 "영향 전수" 절 참조). 에러·머지 바는 타이머 대상이 아니다(notice 값만 구독).

**Files:**
- Modify: `apps/desktop/src/renderer/src/store/repository-store.ts` (NOTICE_TTL_MS·clearNotice)
- Modify: `apps/desktop/src/renderer/src/App.tsx` (타이머 effect)
- Modify: `apps/desktop/e2e/smoke.spec.ts` (+1 — page.clock, 실측 3)

- [x] **Step 1: store.** `apps/desktop/src/renderer/src/store/repository-store.ts` 기존:

```ts
/** IPC assertLimit와 동일한 상한 — 이 이상은 더 불러오지 않는다 */
const HISTORY_MAX = 10000
```

교체:

```ts
/** IPC assertLimit와 동일한 상한 — 이 이상은 더 불러오지 않는다 */
const HISTORY_MAX = 10000
/** notice 자동 소멸 시간(ms) — 타이머는 renderer(App)가 건다. 에러·머지 바에는 적용하지 않는다 (E1d 후속) */
export const NOTICE_TTL_MS = 10_000
```

그리고 인터페이스의 기존:

```ts
  /** 전역 에러를 지운다 — 다이얼로그를 새로 열 때 이전 작업 에러가 인라인으로 새어들지 않게. 동기라 guard 불필요 */
  clearError(): void
```

교체:

```ts
  /** 전역 에러를 지운다 — 다이얼로그를 새로 열 때 이전 작업 에러가 인라인으로 새어들지 않게. 동기라 guard 불필요 */
  clearError(): void
  /** notice만 지운다 — 자동 소멸 타이머(App) 전용. 동기라 guard 불필요 */
  clearNotice(): void
```

그리고 구현부의 기존:

```ts
  clearError() {
    set({ error: null })
  },
```

교체:

```ts
  clearError() {
    set({ error: null })
  },

  clearNotice() {
    set({ notice: null })
  },
```

- [x] **Step 2: App 타이머.** `apps/desktop/src/renderer/src/App.tsx` 기존:

```ts
import { useRepositoryStore } from './store/repository-store'
```

교체:

```ts
import { NOTICE_TTL_MS, useRepositoryStore } from './store/repository-store'
```

그리고 기존:

```tsx
  useEffect(() => {
    void store.init()
    // 마운트 시 1회만 실행
    // eslint-disable-next-line react-hooks/exhaustive-deps
  }, [])
```

교체:

```tsx
  useEffect(() => {
    void store.init()
    // 마운트 시 1회만 실행
    // eslint-disable-next-line react-hooks/exhaustive-deps
  }, [])

  // notice만 10초 뒤 자동으로 사라진다 — 에러·머지 바는 남는다 (E1d 후속). 새 notice가 오면
  // 값이 바뀌어 effect가 다시 걸리며 타이머가 리셋된다. 같은 문구가 연속으로 와도 guard가
  // 작업 시작마다 notice를 null로 비우므로 null→값 전이로 반드시 리셋된다
  useEffect(() => {
    if (store.notice === null) return
    const timer = window.setTimeout(() => store.clearNotice(), NOTICE_TTL_MS)
    return () => window.clearTimeout(timer)
    // store 객체는 렌더마다 새 참조 — notice 값 변화에만 반응해야 임의 갱신이 타이머를 연장하지 않는다
    // eslint-disable-next-line react-hooks/exhaustive-deps
  }, [store.notice])
```

- [x] **Step 3: E2E — 대기 없는 소멸 검증(page.clock, 실측 3).** `apps/desktop/e2e/smoke.spec.ts` 기존:

```ts
test('저장 실행취소 (undo)와 메시지 고치기 (amend)', async () => {
```

앞에 추가:

```ts
test('notice 안내는 10초 뒤 자동으로 사라진다 (에러·머지 바와 달리)', async () => {
  const repo = await createRepoWithChange()
  await execGitOrThrow(['checkout', '--', 'app.txt'], { cwd: repo })
  const app = await electron.launch({
    args: [APP_ROOT],
    env: { ...process.env, GIT_GUI_E2E_REPO: repo },
  })
  try {
    const window = await app.firstWindow()
    await expect(window.getByTestId('history-count')).toHaveText('1')
    // 가짜 시계 — 10초를 실제로 기다리지 않는다 (플랜 실측 3: Electron 페이지에서 clock 동작 확인)
    await window.clock.install()
    await window.locator('[data-testid^="history-item-"]').first().click({ button: 'right' })
    await window.getByTestId('context-tag-here').click()
    await window.getByTestId('prompt-input').fill('v-auto')
    await window.getByTestId('prompt-submit').click()
    await expect(window.getByTestId('notice')).toContainText('태그를 만들었어요')
    await window.clock.fastForward(10_500)
    await expect(window.getByTestId('notice')).toHaveCount(0)
  } finally {
    await app.close()
    await rm(repo, { recursive: true, force: true })
  }
})

```

- [x] **Step 4: 게이트** — 루트 `pnpm test` → **359 passed**(무변). `pnpm typecheck` Done. `cd apps/desktop && npx electron-vite build && npx playwright test e2e/smoke.spec.ts` → **38 passed**. hosting 스펙 notice 단언 2건은 "영향 전수" 표대로 무영향 — 최종 게이트에서 전체 재확인.

- [x] **Step 5: Commit**

```bash
git add apps/desktop/src/renderer/src/store/repository-store.ts apps/desktop/src/renderer/src/App.tsx apps/desktop/e2e/smoke.spec.ts
git commit -m "feat(desktop): E6b notice 10초 자동 소멸 — App 타이머(값 전이 리셋)·page.clock E2E

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>"
```

---

### Task 4: 팝오버·PromptDialog ESC 불응 해소 (실측 2 기반)

두 갈래 수정 — 원인이 서로 다르다(실측 2):

1. **PromptDialog**: TextField `onKeyDown`이 react-aria `createEventHandler`의 기본 전파 차단에 걸린다 → 제출(Enter)이 아닌 키는 `continuePropagation()`으로 전파를 이어 ModalOverlay의 ESC 핸들러에 닿게 한다. **임시 적용·실기동으로 검증 완료**(포커스 input·body 양쪽 모두 ESC 1회로 닫힘).
2. **팝오버(보관함·리뷰·스위처)**: busy 비활성화로 포커스가 body에 떨어지면 keydown 경로에 오버레이가 없다 → **포커스가 body일 때만** 동작하는 문서 수준 fallback 훅. react-aria가 처리한 ESC는 stopPropagation으로 window listener까지 오지 않아(실측) 이중 닫힘이 없다. ShelfPopover는 임시 적용·실기동 검증 완료, 리뷰·스위처는 동일 기전 적용.

**Files:**
- Create: `apps/desktop/src/renderer/src/ui/use-escape-fallback.ts`
- Modify: `apps/desktop/src/renderer/src/ui/PromptDialog.tsx`
- Modify: `apps/desktop/src/renderer/src/components/ShelfPopover.tsx`
- Modify: `apps/desktop/src/renderer/src/components/ReviewPopover.tsx`
- Modify: `apps/desktop/src/renderer/src/components/BranchSwitcher.tsx`
- Modify: `apps/desktop/e2e/smoke.spec.ts` (+1)

- [x] **Step 1: fallback 훅 생성.** `apps/desktop/src/renderer/src/ui/use-escape-fallback.ts` 신규:

```ts
import { useEffect } from 'react'

/**
 * react-aria 오버레이의 ESC 사각지대 보완 (E1a 잔여·E3b 리뷰 — 플랜 실측 2).
 * react-aria의 ESC 닫기는 오버레이 DOM 요소에 부착된 onKeyDown이라, busy로 포커스된
 * 버튼이 비활성화되어 포커스가 body로 떨어지면 keydown 경로에 오버레이가 없어 죽는다.
 * 포커스가 body일 때만 문서 수준에서 받아 닫는다 — react-aria가 처리한 ESC는
 * stopPropagation으로 여기까지 오지 않으므로(실측) 이중 닫힘이 없다.
 */
export function useEscapeFallback(isOpen: boolean, onClose: () => void): void {
  useEffect(() => {
    if (!isOpen) return
    const onKeyDown = (event: KeyboardEvent) => {
      if (event.key !== 'Escape' || document.activeElement !== document.body) return
      onClose()
    }
    window.addEventListener('keydown', onKeyDown)
    return () => window.removeEventListener('keydown', onKeyDown)
  }, [isOpen, onClose])
}
```

- [x] **Step 2: PromptDialog — 전파 유지.** `apps/desktop/src/renderer/src/ui/PromptDialog.tsx` 기존:

```tsx
            onKeyDown={(event) => {
              if (isSubmitEnter(event.key, event.nativeEvent.isComposing)) submit()
            }}
```

교체:

```tsx
            onKeyDown={(event) => {
              if (isSubmitEnter(event.key, event.nativeEvent.isComposing)) {
                submit()
                return
              }
              // react-aria는 onKeyDown이 있으면 기본으로 전파를 끊는다(createEventHandler — 실측 2).
              // ESC가 ModalOverlay의 닫기 핸들러에 닿도록 제출이 아닌 키는 전파를 잇는다
              event.continuePropagation()
            }}
```

- [x] **Step 3: ShelfPopover.** `apps/desktop/src/renderer/src/components/ShelfPopover.tsx` 기존:

```ts
import { useState } from 'react'
```

교체:

```ts
import { useState } from 'react'
import { useEscapeFallback } from '../ui/use-escape-fallback'
```

그리고 기존:

```tsx
  // 미리보기 클릭 시 닫아야 해서 제어형으로 둔다 — 그 외 동작은 기존과 같다
  const [open, setOpen] = useState(false)
```

교체:

```tsx
  // 미리보기 클릭 시 닫아야 해서 제어형으로 둔다 — 그 외 동작은 기존과 같다
  const [open, setOpen] = useState(false)
  // 버리기 확인창이 위에 떠 있는 동안에는 ESC를 확인창에 양보한다 (E6b 실측 2 — body 포커스 fallback)
  useEscapeFallback(open && dropTarget === null, () => setOpen(false))
```

- [x] **Step 4: ReviewPopover.** `apps/desktop/src/renderer/src/components/ReviewPopover.tsx` 기존:

```ts
import { useState } from 'react'
```

교체:

```ts
import { useState } from 'react'
import { useEscapeFallback } from '../ui/use-escape-fallback'
```

그리고 기존:

```tsx
  // 다이얼로그를 여는 동작은 팝오버를 닫고 시작한다 — 모달과 팝오버의 포커스 경합을 피한다
  const [open, setOpen] = useState(false)
```

교체:

```tsx
  // 다이얼로그를 여는 동작은 팝오버를 닫고 시작한다 — 모달과 팝오버의 포커스 경합을 피한다
  const [open, setOpen] = useState(false)
  // busy 비활성화로 포커스가 body에 떨어진 뒤에도 ESC로 닫힌다 (E6b 실측 2)
  useEscapeFallback(open, () => setOpen(false))
```

- [x] **Step 5: BranchSwitcher — 제어형 전환 + fallback.** `apps/desktop/src/renderer/src/components/BranchSwitcher.tsx` 기존:

```ts
import { Check, ChevronDown, Folder, Plus } from 'lucide-react'
import { Header, Menu, MenuItem, MenuSection, MenuTrigger, Popover } from 'react-aria-components'
```

교체:

```ts
import { Check, ChevronDown, Folder, Plus } from 'lucide-react'
import { useState } from 'react'
import { Header, Menu, MenuItem, MenuSection, MenuTrigger, Popover } from 'react-aria-components'
import { useEscapeFallback } from '../ui/use-escape-fallback'
```

그리고 기존:

```tsx
export function BranchSwitcher({ branches, currentName, busy, onSwitch, onCreate, onManage }: BranchSwitcherProps) {
  const grouped = groupBranches(branches)
```

교체:

```tsx
export function BranchSwitcher({ branches, currentName, busy, onSwitch, onCreate, onManage }: BranchSwitcherProps) {
  // ESC fallback을 걸기 위해 제어형으로 둔다 — 그 외 동작은 기존과 같다 (ShelfPopover 관례)
  const [open, setOpen] = useState(false)
  useEscapeFallback(open, () => setOpen(false))
  const grouped = groupBranches(branches)
```

그리고 기존:

```tsx
  return (
    <MenuTrigger>
```

교체:

```tsx
  return (
    <MenuTrigger isOpen={open} onOpenChange={setOpen}>
```

- [x] **Step 6: E2E — ESC로 닫기.** `apps/desktop/e2e/smoke.spec.ts` 기존:

```ts
test('보관함 항목을 클릭하면 담긴 내용을 미리 보여준다', async () => {
```

앞에 추가:

```ts
test('팝오버·입력 다이얼로그가 ESC로 닫힌다 (포커스가 body로 빠진 뒤에도)', async () => {
  const repo = await createRepoWithChange()
  const app = await electron.launch({
    args: [APP_ROOT],
    env: { ...process.env, GIT_GUI_E2E_REPO: repo },
  })
  try {
    const window = await app.firstWindow()
    // (1) 보관함 팝오버 — 보관하기(busy 비활성화 사이클)로 포커스가 body로 떨어진 상태의 ESC (E1a 잔여 재현)
    await window.getByTestId('shelf-open').click()
    await window.getByTestId('shelf-save').click()
    await expect(window.getByTestId('shelf-count')).toHaveText('1')
    await window.keyboard.press('Escape')
    await expect(window.getByTestId('shelf-save')).toHaveCount(0)
    // (2) PromptDialog — 입력에 포커스가 있어도 ESC로 닫힌다 (E3b 리뷰 재현: TextField 기본 전파 차단)
    await window.locator('[data-testid^="history-item-"]').first().click({ button: 'right' })
    await window.getByTestId('context-tag-here').click()
    await expect(window.getByTestId('prompt-input')).toBeVisible()
    await window.keyboard.press('Escape')
    await expect(window.getByTestId('prompt-input')).toHaveCount(0)
  } finally {
    await app.close()
    await rm(repo, { recursive: true, force: true })
  }
})

```

- [x] **Step 7: 게이트** — 루트 `pnpm test` → **359 passed**. `pnpm typecheck` Done. `cd apps/desktop && npx electron-vite build && npx playwright test e2e/smoke.spec.ts` → **39 passed**(신규 ESC 1건 포함 — 특히 기존 '실험 공간을 만들고 바로 이동한다'·'실험 공간 이름을 바꾼다'·태그·reword 등 PromptDialog Enter/제출 경로와 스위처 경유 흐름 전부 통과 = continuePropagation·제어형 전환의 무회귀 확인).

- [x] **Step 8: Commit**

```bash
git add apps/desktop/src/renderer/src/ui/use-escape-fallback.ts apps/desktop/src/renderer/src/ui/PromptDialog.tsx apps/desktop/src/renderer/src/components/ShelfPopover.tsx apps/desktop/src/renderer/src/components/ReviewPopover.tsx apps/desktop/src/renderer/src/components/BranchSwitcher.tsx apps/desktop/e2e/smoke.spec.ts
git commit -m "fix(desktop): E6b ESC 불응 해소 — TextField 전파 유지(continuePropagation)·body 포커스 fallback 훅

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>"
```

---

### Task 5: E2E 디버깅 경로 — GIT_GUI_E2E_SHOW + 실패 시 마지막 화면 저장

설계(실측 4 근거): expect 실패가 테스트 finally를 지나는 시점에는 `test.info()`가 실패를 모른다(status 'passed') — 기존 43건의 "finally에서 app.close()" 구조를 유지한 채 실패 시점 화면을 남기려면 **close 직전에 항상 찍고 afterEach(testInfo.status)가 성공분을 지우는** 방식뿐이다. harness가 `_electron.launch`를 감싸 close를 래핑하므로 **스펙 수정은 import 1줄 + afterEach 3줄씩**로 끝난다(launch/close 호출부 46곳 무수정). afterEach를 harness 모듈 부수효과로 등록하지 않는 이유: 워커가 두 스펙 파일을 이어 실행하면 모듈 캐시 때문에 두 번째 파일에 훅이 등록되지 않을 수 있다 — **각 스펙에 명시 등록**한다. 스크린샷 비용은 테스트당 ~0.1초(전체 +5초 미만).

**Files:**
- Modify: `apps/desktop/src/main/index.ts` (GIT_GUI_E2E_SHOW)
- Create: `apps/desktop/e2e/harness.ts`
- Modify: `apps/desktop/e2e/smoke.spec.ts`, `apps/desktop/e2e/hosting.spec.ts` (import·afterEach·첫 테스트 가드)

- [x] **Step 1: main — 표시 opt-out.** `apps/desktop/src/main/index.ts` 기존:

```ts
const isE2E = !app.isPackaged && process.env.GIT_GUI_E2E_REPO !== undefined
```

교체:

```ts
const isE2E = !app.isPackaged && process.env.GIT_GUI_E2E_REPO !== undefined
// 로컬 디버깅 opt-out (E6a 후속) — GIT_GUI_E2E_SHOW=1이면 숨김 게이트만 무시하고 창을 보여준다.
// 스로틀 해제 등 나머지 E2E 동작은 유지. 프로덕션(isPackaged)·CI 기본 동작 무변
const isE2EShow = isE2E && process.env.GIT_GUI_E2E_SHOW === '1'
```

그리고 기존:

```ts
    show: !isE2E,
```

교체:

```ts
    show: !isE2E || isE2EShow,
```

그리고 기존:

```ts
if (isE2E) app.dock?.hide()
```

교체:

```ts
if (isE2E && !isE2EShow) app.dock?.hide()
```

- [x] **Step 2: harness 생성.** `apps/desktop/e2e/harness.ts` 신규:

```ts
import { existsSync } from 'node:fs'
import { rm } from 'node:fs/promises'
import { _electron, test, type ElectronApplication, type TestInfo } from '@playwright/test'

/**
 * 실패 시각 단서 (E6a 후속) — 숨김 창 E2E는 실패해도 화면 단서가 없다(electron.launch에는
 * Playwright 자동 trace 스크린샷이 붙지 않는다). 실측(플랜 실측 4): expect 실패가 테스트
 * 본문 finally를 지나는 시점에는 test.info().status가 아직 'passed'라 "실패일 때만 찍기"가
 * 불가능하다 — close 직전에 항상 마지막 화면을 찍고, 각 스펙의 afterEach(cleanupScreens)가
 * 성공한 테스트의 것만 지운다. 실패 시에만 test-results/<테스트 폴더>/last-screen-N.png가 남는다.
 */
const captured: string[] = []
let captureIndex = 0

export const electron = {
  /** _electron.launch 래퍼 — close를 감싸 닫기 직전 화면을 저장한다. 사용법은 _electron.launch와 동일 */
  async launch(
    options: NonNullable<Parameters<typeof _electron.launch>[0]>,
  ): Promise<ElectronApplication> {
    const app = await _electron.launch(options)
    const close = app.close.bind(app)
    app.close = async () => {
      captureIndex += 1
      const path = test.info().outputPath(`last-screen-${captureIndex}.png`)
      // 창이 이미 죽었어도 닫기는 계속한다 — 진단 보조일 뿐 테스트를 실패시키지 않는다
      await app
        .firstWindow()
        .then((window) => window.screenshot({ path }))
        .catch(() => {})
      captured.push(path)
      return close()
    }
    return app
  },
}

/** 각 스펙의 test.afterEach에서 호출 — 성공(기대 일치) 테스트의 화면 파일을 지운다. timeout 실패도 남는다 */
export async function cleanupScreens(testInfo: TestInfo): Promise<void> {
  if (testInfo.status === testInfo.expectedStatus) {
    for (const path of captured) {
      if (existsSync(path)) await rm(path, { force: true })
    }
  }
  captured.length = 0
}
```

- [x] **Step 3: smoke 배선.** `apps/desktop/e2e/smoke.spec.ts` 기존:

```ts
import { _electron as electron, expect, test } from '@playwright/test'
```

교체:

```ts
import { expect, test } from '@playwright/test'
import { cleanupScreens, electron } from './harness'
```

그리고 기존:

```ts
// cwd에 의존하지 않도록 앱 루트를 절대 경로로 지정한다
const APP_ROOT = join(__dirname, '..')
```

교체:

```ts
// cwd에 의존하지 않도록 앱 루트를 절대 경로로 지정한다
const APP_ROOT = join(__dirname, '..')

// 실패한 테스트만 마지막 화면(last-screen-N.png)이 남는다 — harness가 close 직전마다 찍은 것을 정리
test.afterEach(async ({}, testInfo) => {
  await cleanupScreens(testInfo)
})
```

그리고 첫 테스트의 기존:

```ts
    // E2E 창 비간섭(E6a) — 창은 숨긴 채 CDP로만 조작한다. 회귀하면 e2e마다 작업 화면을 가린다
    expect(
      await app.evaluate(({ BrowserWindow }) => BrowserWindow.getAllWindows()[0]!.isVisible()),
    ).toBe(false)
```

교체:

```ts
    // E2E 창 비간섭(E6a) — 창은 숨긴 채 CDP로만 조작한다. 회귀하면 e2e마다 작업 화면을 가린다.
    // GIT_GUI_E2E_SHOW=1(로컬 디버깅 opt-out — E6b)은 의도적으로 보이므로 이 가드만 건너뛴다
    if (process.env.GIT_GUI_E2E_SHOW !== '1') {
      expect(
        await app.evaluate(({ BrowserWindow }) => BrowserWindow.getAllWindows()[0]!.isVisible()),
      ).toBe(false)
    }
```

- [x] **Step 4: hosting 배선.** `apps/desktop/e2e/hosting.spec.ts` 기존:

```ts
import { _electron as electron, expect, test } from '@playwright/test'
```

교체:

```ts
import { expect, test } from '@playwright/test'
import { cleanupScreens, electron } from './harness'
```

그리고 기존:

```ts
const APP_ROOT = join(__dirname, '..')
```

교체:

```ts
const APP_ROOT = join(__dirname, '..')

// 실패한 테스트만 마지막 화면(last-screen-N.png)이 남는다 — harness가 close 직전마다 찍은 것을 정리
test.afterEach(async ({}, testInfo) => {
  await cleanupScreens(testInfo)
})
```

- [x] **Step 5: 실패 경로 실증 (임시 — 커밋 금지).** harness의 "실패 시에만 남는다"를 한 번 실증한다. 임시 파일 `apps/desktop/e2e/tmp-harness-check.spec.ts`:

```ts
// 임시 검증 — 실패 시 last-screen이 남는지. 실행 후 즉시 삭제한다 (커밋 금지)
import { mkdtemp, rm, writeFile } from 'node:fs/promises'
import { tmpdir } from 'node:os'
import { join } from 'node:path'
import { expect, test } from '@playwright/test'
import { execGitOrThrow } from '@git-gui/git-process'
import { cleanupScreens, electron } from './harness'

const APP_ROOT = join(__dirname, '..')

test.afterEach(async ({}, testInfo) => {
  await cleanupScreens(testInfo)
})

test.fail(true, '의도적 실패 — 실패 스크린샷 잔존 확인')
test('harness 실패 화면 잔존 검증', async () => {
  const repo = await mkdtemp(join(tmpdir(), 'git-gui-harness-check-'))
  await execGitOrThrow(['init', '--initial-branch=main'], { cwd: repo })
  await execGitOrThrow(['config', 'user.name', 'E2E'], { cwd: repo })
  await execGitOrThrow(['config', 'user.email', 'e2e@test.local'], { cwd: repo })
  await writeFile(join(repo, 'app.txt'), 'v1\n')
  await execGitOrThrow(['add', '-A'], { cwd: repo })
  await execGitOrThrow(['commit', '-m', 'init'], { cwd: repo })
  const app = await electron.launch({
    args: [APP_ROOT],
    env: { ...process.env, GIT_GUI_E2E_REPO: repo },
  })
  try {
    const window = await app.firstWindow()
    await expect(window.getByTestId('history-count')).toHaveText('1')
    expect(1, '의도적 실패').toBe(2)
  } finally {
    await app.close()
    await rm(repo, { recursive: true, force: true })
  }
})
```

실행·확인·삭제:

```bash
cd apps/desktop && npx electron-vite build && npx playwright test e2e/tmp-harness-check.spec.ts
find test-results -name 'last-screen-*.png'   # 1장 존재해야 한다 (test.fail이라 status=failed, expected=failed → 이 케이스는 기대 일치로 삭제됨에 주의)
```

주의: `test.fail(true)`는 expectedStatus도 failed로 만들어 cleanupScreens가 지워 버린다 — **잔존 확인은 test.fail 줄을 빼고 실행**한다(스위트는 1 failed로 끝나는 것이 정상). `last-screen-1.png` 존재를 확인한 뒤:

```bash
rm apps/desktop/e2e/tmp-harness-check.spec.ts
rm -rf apps/desktop/test-results/tmp-harness-check-*
```

- [x] **Step 6: 게이트 (전체 E2E — harness가 43+2건 전부를 지난다)** — `cd apps/desktop && npx electron-vite build && npx playwright test` → **45 passed**(smoke 39 + hosting 6). 종료 후 `find test-results -name 'last-screen-*.png'` → **0건**(성공분 전부 삭제 확인). 로컬 육안 1회: `GIT_GUI_E2E_SHOW=1 npx playwright test e2e/smoke.spec.ts -g "빈 메시지"` — 실행 중 창이 **보이고** 테스트는 통과(첫 테스트의 isVisible 가드는 이 테스트에 없음), 끝나면 창이 닫히는지 확인.

- [x] **Step 7: Commit**

```bash
git add apps/desktop/src/main/index.ts apps/desktop/e2e/harness.ts apps/desktop/e2e/smoke.spec.ts apps/desktop/e2e/hosting.spec.ts
git commit -m "feat(desktop): E6b E2E 디버깅 경로 — GIT_GUI_E2E_SHOW 창 표시·실패 시 마지막 화면 저장(harness)

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>"
```

---

### Task 6: 커밋이 삭제한 파일의 "이 파일만 적용" 비활성 + 사유

**Files:**
- Modify: `apps/desktop/src/renderer/src/components/CommitDetailPanel.tsx`
- Modify: `apps/desktop/e2e/smoke.spec.ts` (+1)

- [x] **Step 1: 메뉴 항목 분기.** `apps/desktop/src/renderer/src/components/CommitDetailPanel.tsx` 기존:

```tsx
          items={[
            {
              key: 'restore-file',
              label: shelfPreview
                ? '이 파일만 꺼내 적용 (checkout)'
                : '이 파일만 지금 코드에 적용 (checkout)',
              onSelect: () => setConfirmingRestore(menu.file),
            },
```

교체:

```tsx
          items={[
            {
              key: 'restore-file',
              // 이 저장이 "지운" 파일은 그 시점 내용이 없다 — 숨기지 않고 사유와 함께 비활성 (E5a 후속).
              // 엔진의 '그 시점에는 이 파일이 없어요' 친절 거부는 심층 방어로 유지된다
              label:
                menu.file.kind === 'deleted'
                  ? '이 파일만 적용 (checkout) — 그 시점에 지워진 파일이에요'
                  : shelfPreview
                    ? '이 파일만 꺼내 적용 (checkout)'
                    : '이 파일만 지금 코드에 적용 (checkout)',
              disabled: menu.file.kind === 'deleted',
              onSelect: () => setConfirmingRestore(menu.file),
            },
```

(HistoryPanel의 undo/reword 메뉴와 동일 관례 — "라벨에 사유 병기 + disabled". ContextMenu는 `disabled` prop을 이미 지원한다.)

- [x] **Step 2: E2E.** `apps/desktop/e2e/smoke.spec.ts` — 파일 맨 끝(마지막 테스트 '저장 실행취소 (undo)와 메시지 고치기 (amend)'의 닫는 `})` 뒤)에 추가:

```ts

test('커밋이 삭제한 파일은 "이 파일만 적용"이 사유와 함께 비활성이다', async () => {
  const repo = await createRepoWithChange()
  await execGitOrThrow(['checkout', '--', 'app.txt'], { cwd: repo })
  await execGitOrThrow(['rm', 'app.txt'], { cwd: repo })
  await execGitOrThrow(['commit', '-m', '파일 정리 저장'], { cwd: repo })
  const app = await electron.launch({
    args: [APP_ROOT],
    env: { ...process.env, GIT_GUI_E2E_REPO: repo },
  })
  try {
    const window = await app.firstWindow()
    await expect(window.getByTestId('history-count')).toHaveText('2')
    // 삭제 커밋(최신) 상세 — app.txt는 kind=deleted로 나온다
    await window.locator('[data-testid^="history-item-"]').first().click()
    await expect(window.getByTestId('commit-detail-panel')).toBeVisible()
    await window.getByTestId('commit-file-app.txt').click({ button: 'right' })
    await expect(window.getByTestId('context-restore-file')).toBeDisabled()
    await expect(window.getByTestId('context-restore-file')).toContainText('지워진 파일이에요')
    // 비교(diff)는 계속 살아 있다 — 지워진 내용도 확인할 수 있다
    await expect(window.getByTestId('context-compare-worktree')).toBeEnabled()
    await window.keyboard.press('Escape')
  } finally {
    await app.close()
    await rm(repo, { recursive: true, force: true })
  }
})
```

- [x] **Step 3: 게이트** — `cd apps/desktop && npx electron-vite build && npx playwright test e2e/smoke.spec.ts` → **40 passed**. 루트 `pnpm test` → 359, `pnpm typecheck` Done.

- [x] **Step 4: Commit**

```bash
git add apps/desktop/src/renderer/src/components/CommitDetailPanel.tsx apps/desktop/e2e/smoke.spec.ts
git commit -m "fix(desktop): E6b 삭제 파일 '이 파일만 적용' 비활성 — 사유 병기(엔진 거부는 심층 방어 유지)

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>"
```

---

### Task 7: 최종 게이트 + 공식 스크린샷 2장 + README

- [x] **Step 1: 전체 게이트** — 순서대로 전부 exit 0:
  - 루트 `pnpm test` → **359 passed**
  - 루트 `pnpm typecheck` → 전 프로젝트 Done
  - `pnpm --filter @git-gui/desktop build`
  - `pnpm --filter @git-gui/desktop e2e` → **46 passed** (smoke 40 + hosting 6, 실행 내내 창 미노출)
  - `find apps/desktop/test-results -name 'last-screen-*.png'` → 0건

- [x] **Step 2: README 최소 반영.** `README.md` 기존:

```
E6a로 화면 배치가 개편되었습니다 — 좌측은 변경 목록과 저장 폼, 중앙은 diff·충돌 뷰 전용, 우측 트리는 항상 보이고 커밋을 클릭하면 하단에만 상세가 열립니다. 창 폭에 따라 좌·우 열이 줄어 960px 최소 창에서도 중앙 diff가 380px 이상을 지키며, E2E 실행 중에는 앱 창이 화면에 나타나지 않습니다.
```

교체:

```
E6a로 화면 배치가 개편되었습니다 — 좌측은 변경 목록과 저장 폼, 중앙은 diff·충돌 뷰 전용, 우측 트리는 항상 보이고 커밋을 클릭하면 하단에만 상세가 열립니다. 창 폭에 따라 좌·우 열이 줄어 960px 최소 창에서도 중앙 diff가 380px 이상을 지키며, E2E 실행 중에는 앱 창이 화면에 나타나지 않습니다. E6b 폴리시로 태그 배지(🏷)가 로컬·원격(☁)과 구분되어 원격 뒤 우선순위로 정렬되고, 안내(notice)는 10초 뒤 자동으로 사라지며(에러·진행 바는 유지), 팝오버·입력 다이얼로그가 ESC로 닫히고, 원격이 앞선 백업(push)은 받아오기(pull) 안내로 풀어 주며, 커밋이 삭제한 파일의 "이 파일만 적용"은 사유와 함께 비활성됩니다. E2E는 실패한 테스트의 마지막 화면을 test-results에 남기고, GIT_GUI_E2E_SHOW=1로 창을 보면서 디버깅할 수 있습니다.
```

- [x] **Step 3: 공식 스크린샷 2장** (`test-results/` + scratchpad `/private/tmp/claude-501/-Users-sangyeop-kim-git-gui/47e198c4-f65c-435f-b962-13de0c0d68a0/scratchpad/` 사본, **생성 후 e2e 재실행 금지** — 재실행하면 test-results가 갈린다). 선택 근거: "e6b-esc-or-toast" 중 **notice 토스트**를 택한다 — ESC 닫힘은 정적 캡처로 검증력이 없고, 토스트는 문구·배치가 한눈에 보인다.

임시 파일 `apps/desktop/e2e/tmp-shots-e6b.spec.ts`를 다음 내용으로 만들고(직접 `_electron` 사용 — harness의 last-screen 잔여물을 만들지 않는다):

```ts
// 임시 파일 — 공식 스크린샷 생성 후 삭제한다 (커밋 금지)
import { copyFile, mkdtemp, rm, writeFile } from 'node:fs/promises'
import { tmpdir } from 'node:os'
import { join } from 'node:path'
import { _electron as electron, expect, test } from '@playwright/test'
import { execGitOrThrow } from '@git-gui/git-process'

const APP_ROOT = join(__dirname, '..')
const SCRATCH =
  '/private/tmp/claude-501/-Users-sangyeop-kim-git-gui/47e198c4-f65c-435f-b962-13de0c0d68a0/scratchpad'

test('공식 스크린샷 — E6b 태그 배지 3분 구분·notice 토스트 2장', async () => {
  const repo = await mkdtemp(join(tmpdir(), 'git-gui-shot-'))
  await execGitOrThrow(['init', '--initial-branch=main'], { cwd: repo })
  await execGitOrThrow(['config', 'user.name', 'E2E'], { cwd: repo })
  await execGitOrThrow(['config', 'user.email', 'e2e@test.local'], { cwd: repo })
  await writeFile(join(repo, 'app.txt'), 'v1\n')
  await execGitOrThrow(['add', '-A'], { cwd: repo })
  await execGitOrThrow(['commit', '-m', '첫 화면 저장'], { cwd: repo })
  await execGitOrThrow(['tag', 'v1.0'], { cwd: repo })
  await writeFile(join(repo, 'app.txt'), 'v2\n')
  await execGitOrThrow(['commit', '-am', '백업된 저장'], { cwd: repo })
  const remote = await mkdtemp(join(tmpdir(), 'git-gui-shot-remote-'))
  await execGitOrThrow(['init', '--bare', '--initial-branch=main'], { cwd: remote })
  await execGitOrThrow(['remote', 'add', 'origin', remote], { cwd: repo })
  await execGitOrThrow(['push', '-u', 'origin', 'main'], { cwd: repo })
  await writeFile(join(repo, 'app.txt'), 'v3\n')
  await execGitOrThrow(['commit', '-am', '이어서 작업한 저장'], { cwd: repo })
  const app = await electron.launch({
    args: [APP_ROOT],
    env: { ...process.env, GIT_GUI_E2E_REPO: repo },
  })
  try {
    const window = await app.firstWindow()
    await app.evaluate(({ BrowserWindow }) => {
      BrowserWindow.getAllWindows()[0]!.setSize(1440, 900)
    })
    await expect.poll(() => window.evaluate(() => window.innerWidth)).toBe(1440)
    // (1) 태그·원격·로컬 3분 구분 — main(민무늬) / ☁ origin/main(점선) / 🏷 v1.0
    await expect(window.getByTestId('history-list')).toContainText('main')
    await expect(window.getByTestId('history-list')).toContainText('☁ origin/main')
    await expect(window.getByTestId('history-list')).toContainText('🏷 v1.0')
    await window.screenshot({ path: 'test-results/e6b-tag-badges.png' })
    // (2) notice 토스트 — 태그 만들기 안내가 떠 있는 순간 (10초 안에 촬영)
    await window.locator('[data-testid^="history-item-"]').first().click({ button: 'right' })
    await window.getByTestId('context-tag-here').click()
    await window.getByTestId('prompt-input').fill('v2.0')
    await window.getByTestId('prompt-submit').click()
    await expect(window.getByTestId('notice')).toContainText('태그를 만들었어요')
    await window.screenshot({ path: 'test-results/e6b-notice-toast.png' })
    await copyFile('test-results/e6b-tag-badges.png', join(SCRATCH, 'e6b-tag-badges.png'))
    await copyFile('test-results/e6b-notice-toast.png', join(SCRATCH, 'e6b-notice-toast.png'))
  } finally {
    await app.close()
    await rm(repo, { recursive: true, force: true })
    await rm(remote, { recursive: true, force: true })
  }
})
```

실행·정리 (build는 Step 1에서 이미 됐다 — 재빌드 없이 이 파일만 실행):

```bash
cd apps/desktop && npx playwright test e2e/tmp-shots-e6b.spec.ts
rm apps/desktop/e2e/tmp-shots-e6b.spec.ts
```

스크린샷 2장(`e6b-tag-badges.png`·`e6b-notice-toast.png`)이 test-results/와 scratchpad 양쪽에 있는지 확인하고 **육안 검수**한다: (a) e6b-tag-badges — 세 행에 각각 `main`(민무늬)·`☁ origin/main`(점선 테두리)·`🏷 v1.0`이 서로 다른 모양으로 보이는지, (b) e6b-notice-toast — 상단 배너에 "v2.0 태그를 만들었어요"가 겹침·잘림 없이 보이고 히스토리에 `🏷 v2.0` 배지가 함께 있는지. 이후 e2e를 다시 돌리지 않는다.

- [x] **Step 4: Commit** (README만 — 스크린샷·test-results/는 미추적)

```bash
git add README.md
git commit -m "docs: README — E6b 폴리시(태그 배지·notice 자동 소멸·ESC·백업 안내·E2E 디버깅) 반영

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>"
```

---

## 검증 게이트 요약

| 시점 | 기대치 |
| --- | --- |
| 기준선 (5bfeefe, 실측) | **353 tests**(27 files) + E2E **43** (smoke 37 + hosting 6) |
| Task 1 후 | +3 → **356 tests** (push 친절화 3케이스 — Red 실증 후 Green) |
| Task 2 후 | +3 → **359 tests** + typecheck Done + smoke 37(태그 단언 강화 통과) |
| Task 3 후 | 359 유지 + smoke **38** (notice 소멸 — page.clock, 실제 대기 0초) |
| Task 4 후 | 359 유지 + smoke **39** (ESC — 팝오버 body 포커스·PromptDialog) |
| Task 5 후 | 전체 E2E **45**(smoke 39 + hosting 6) + last-screen 잔존/삭제 실증 + SHOW=1 육안 |
| Task 6 후 | smoke **40** (삭제 파일 disabled) |
| 최종 (Task 7) | **359 tests** + typecheck + build + E2E **46**(smoke 40 + hosting 6) — 전부 exit 0 + last-screen 0건 + 스크린샷 2장(육안 검수) + README |

## 인용 앵커 검증 기록

플랜의 "기존:" 코드 블록은 전부 대상 파일에서 **정확히 1회** 매칭됨을 확인했다(2026-07-21, 5bfeefe — 단일 행은 `grep -cF`, 다중 행은 python 부분 문자열 카운트): client.test.ts의 detached HEAD 테스트 5줄 블록, client.ts의 `async push()` 전체 함수(42줄 — `async push() {` 자체가 파일 내 유일), repository.ts의 refs 필드 2줄, log-parser.test.ts의 `committedAt: 1752561600` 3줄·refs 단언 3줄·grafted 테스트 4줄, history-graph.test.ts의 `committedAt: 0` 3줄, HistoryPanel.tsx의 import 줄·배지 IIFE 36줄 블록, smoke.spec.ts의 태그 단언 2줄·`test('저장 실행취소 (undo)와 메시지 고치기 (amend)'` 제목 줄·`test('보관함 항목을 클릭하면` 제목 줄·`import { _electron as electron` 줄·APP_ROOT 2줄 블록·isVisible 4줄 블록·undo 테스트 꼬리(`고친 첫 저장`), repository-store.ts의 HISTORY_MAX 2줄·clearError 선언 2줄·clearError 구현 3줄, App.tsx의 store import 줄·init effect 5줄 블록, PromptDialog.tsx의 onKeyDown 3줄 블록, ShelfPopover.tsx·ReviewPopover.tsx의 `import { useState } from 'react'` 줄(각 파일 내 유일)·open 상태 2줄 블록(주석 포함으로 유일화), BranchSwitcher.tsx의 import 2줄·함수 시그니처 2줄·`<MenuTrigger>` 2줄, main/index.ts의 isE2E 줄·`show: !isE2E,` 줄·dock 줄, hosting.spec.ts의 import 줄·APP_ROOT 줄, CommitDetailPanel.tsx의 items 배열 선두 8줄 블록, README.md의 E6a 문장. 교체 후 앵커가 재등장해 순서 의존이 생기는 곳은 없다(각 파일 내 앵커는 상호 독립).

## Self-review 수정 기록 (인라인 반영)

1. **push 판정 문자열을 `failed to push some refs`에서 괄호 사유로 교체** — 초안은 후속 노트 원문의 `failed to push some refs`를 그대로 쓰려 했으나, 이 문자열은 pre-receive hook 거부(`[remote rejected]`)에도 나와 "원격에 새 저장" 오안내가 된다. 실측 4케이스 공통이면서 hook 케이스에 없는 `(fetch first)`·`(non-fast-forward)`로 좁혔다(실측 1에 근거 병기).
2. **harness afterEach를 모듈 부수효과로 등록하는 초안 폐기** — Playwright 워커가 두 스펙을 이어 실행하면 harness 모듈이 캐시되어 두 번째 파일에 훅이 등록되지 않을 수 있다. `cleanupScreens` export + **각 스펙 명시 등록**으로 변경(Task 5 설계 문단에 사유 명기).
3. **"실패일 때만 스크린샷" 초안 폐기 — 실측으로 반증** — finally 시점 `test.info().status`가 'passed'임을 실측(실측 4)해, launch/close 46곳 리팩터링 없이 성립하는 "항상 찍고 성공분 삭제" 설계로 확정. 스펙 수정을 import 1줄+afterEach 3줄로 최소화.
4. **ShelfPopover fallback에 dropTarget 가드 추가** — 버리기 확인창(ConfirmDialog)이 팝오버 위에 떠 있을 때 body 포커스 ESC가 아래 팝오버를 닫아 버리는 중첩 순서 역전을 막았다(`open && dropTarget === null`).
5. **tags 설계에서 "refs에서 태그 제외" 안 기각** — switchTarget(`HistoryPanel.tsx:185`) 등 refs 소비자의 의미가 바뀌는 파급이 있어, refs 완전 불변 + tags 병행(마커) 안으로 확정. 파급 전수 표에 소비자별 판정 명기. 동명 브랜치·태그 중복 키는 기존에도 있던 한계로 후속 노트 이관.
6. **notice 동일 문구 연속 시 타이머 미리셋 우려 해소** — zustand는 같은 값 set에 리렌더가 없지만, 모든 notice는 guard의 `notice: null` 선세팅을 거쳐 null→값 전이가 강제되므로 effect가 반드시 재실행된다(코드 실독 — notice를 세팅하는 경로 전부 guard 내부). Task 3 주석에 근거 병기.
7. **notice E2E에서 clock.install을 notice 발생 전에 걸도록 고정** — 타이머는 notice set 시점에 만들어지므로 install이 늦으면 진짜 10초를 기다리게 된다. 테스트 코드에서 launch 직후 install로 배치.
8. **BranchSwitcher 제어형 전환의 회귀 면 확인** — MenuTrigger controlled(isOpen/onOpenChange)에서 항목 액션·바깥 클릭 닫힘은 RAC 상태 머신이 onOpenChange(false)로 알린다. 기존 E2E 5곳(스위처 경유 흐름: 만들기·전환 2건·관리 2건)이 게이트로 커버함을 Task 4 Step 7에 명기.
9. **6건 커버리지·placeholder 스캔·시그니처 점검** — 확정 범위 6건 전부 태스크 1~6에 1:1 매핑(대응표), 코드 블록에 생략 부호·placeholder 없음, `arrangeRefs`(기본 인자 추가 — 기존 호출 무영향)·`GitResult`/`GitError` import 존재·`continuePropagation` 타입 존재(@react-types/shared 3.36.0)·`ContextMenuItem.disabled` 지원을 실파일 실독으로 확인. Task 1 교체 블록의 의도적 오타 경고(`HEEAD`)는 실수 방지용 주기 — 구현 시 바로잡는 줄을 명기했다.

## 후속 노트 (이관 후보)

- **동명 브랜치·태그 장식 중복** — `main, tag: main`이면 refs에 동명 2건(React key 중복, 기존 한계). tags 분리 렌더 또는 dedupe 검토.
- **notice 수동 닫기 버튼 부재** — 10초를 기다리지 않고 지우는 × 버튼(접근성: role="status"와 함께) 검토.
- **notice 연속 갱신 시 잔여 시간 표시 없음** — 토스트 진행 바(줄어드는 밑줄) 검토.
- **ESC fallback의 적용 범위** — ConfirmDialog·ListDialog·ManageBranchesDialog는 onKeyDown이 없어 이번 결함 면에는 없지만, body 포커스 사각지대 자체는 이론상 공유한다. 문제가 관측되면 같은 훅을 확대.
- **react-aria 업그레이드 시 재검** — `createEventHandler`의 기본 stopPropagation·`useOverlay`의 오버레이 부착 ESC는 라이브러리 내부 동작이다. 버전 올릴 때 ESC E2E(스모크 1건)가 회귀 게이트.
- **last-screen은 close 시점 화면** — 실패 "순간"이 아니라 finally 직전 상태다(대부분 동일하나 finally에서 상태를 바꾸는 테스트는 어긋날 수 있다). 실패 순간 캡처가 필요해지면 expect 래퍼 검토.
- **GIT_GUI_E2E_SHOW=1로 스위트 전체 실행 시 첫 스모크의 isVisible 가드는 건너뛴다** — 디버깅 전용 모드라는 전제. CI에는 이 env를 넣지 않는다.
- **push 친절화의 잔여 케이스** — `[remote rejected]`(hook·권한)는 여전히 GitError 원문이다. 발생 사례가 모이면 별도 문구 검토.
- **태그 우선순위와 +N 접기** — 태그가 원격 뒤라 접힘 시 태그는 대부분 툴팁으로만 보인다. 태그 전용 필터/토글 검토.

## 실행 기록 (2026-07-21, subagent-driven — 태스크별 스펙 byte-match 리뷰 + 품질 리뷰 + 최종 통합 리뷰 전부 통과)

- 커밋 7건: 85904ec(T1) · a6dd6a7(T2) · 7093d64(T3) · 96bac2a(T4) · a5048fc(T5) · e061a13(T6) · 355ea44(T7 README). 최종 게이트 실측: 단위 **360**, typecheck 전부 Done, E2E **46**(smoke 40 + hosting 6), last-screen 0건, 스크린샷 2장 육안 검수 통과.
- **게이트 수치 정정 (플랜 오기):** 위 본문·게이트 표의 "359 tests"는 산수 오기다 — Task 2 Step 6 블록이 history-refs 테스트를 +2가 아니라 **+3**(하위 호환 테스트 포함) 추가하므로 356+4=**360**이 맞다. 스펙 리뷰가 기준 커밋 worktree 실측(356)으로 교차 확인했다. 코드는 플랜 블록과 byte-exact.

### 리뷰 Minor 후속 노트 (이관 후보 — 전부 비차단)

- **(T2 품질) 태그>원격>로컬 판정이 3곳에 분산** — refPriority·refBadgeLabel·HistoryPanel 클래스 조건이 각자 `tags.includes`로 재판정. `refBadgeKind(ref, currentBranch, tags)` 단일 분류기로 모으면 우선순위 변경이 한 곳으로 끝난다. 동명 브랜치·태그 충돌 시 value-매칭이라 두 배지 모두 🏷·태그 우선순위가 되는 점도 같은 자리에서 해소 가능(기존 "동명 장식 중복" 노트의 증상 보강).
- **(T3 품질) syncAfterMerge의 notice 덧붙임이 guard 밖 유일한 notice 쓰기** — 값이 바뀌어 타이머는 정상 리셋되지만, "모든 notice 쓰기는 guard 경유" 불변식의 단독 예외라 한 줄 주석 권고. role="status" 배너의 조건부 마운트는 일부 SR에서 announce 누락 여지 — 기존 "수동 닫기 버튼" 후속에 상시 마운트+텍스트 교체 전환을 병합.
- **(T4 품질) ESC fallback 게이트 강건화** — `activeElement === document.body`만 보는 게이트를 documentElement·null까지 넓히면 Chromium 포커스 폴백 변화에 공짜로 강건해진다. 또 한글 IME 조합 중 ESC가 조합 취소와 동시에 다이얼로그를 닫는다(제품 결정 필요 — 조합 중엔 `continuePropagation` 보류안: `if (!event.nativeEvent.isComposing)`).
- **(T5 품질) harness JSDoc "timeout 실패도 남는다"는 과대 서술** — 스크린샷은 close() 도달 실패만 남는다(본문이 finally 전에 중단되는 hard timeout·crash는 캡처 없음). 문구 정정 권고. 스크린샷 실패의 silent catch에 console.warn, double-close 미가드도 저비용 강화 후보.
- **(T6 품질) ContextMenu 긴 라벨** — 삭제 사유 병기 라벨(~30자)이 우측 열 가장자리 240px 클램프 상수보다 넓어 줄바꿈될 수 있다(코스메틱). smoke 주석 "지워진 내용도 확인할 수 있다"는 부정확 — 삭제 커밋 자체와의 diff는 빈 결과라 내용 확인은 부모 커밋 비교가 필요. shelfPreview의 삭제 라벨이 커밋 어조("그 시점")를 쓰는 어조 불일치도 옵션.
- **(통합 리뷰) useEscapeFallback가 열려 있는 동안 렌더마다 리스너 재구독** — onClose 인라인 화살표가 deps에 있어 재구독 churn. 기능 무해·프로젝트의 no-useCallback 규칙과 정합이라 수정 불요(React Compiler 미도입 상태임을 인지만).
